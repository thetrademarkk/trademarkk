# Commit gate — nothing lands without passing all of these.
echo "1/3 lint-staged (eslint --fix + prettier on staged files)"
npx lint-staged || exit 1

echo "2/3 typecheck"
npm run typecheck || exit 1

echo "3/3 unit tests"
npm test || exit 1
