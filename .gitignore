# dependencies
/node_modules

# market data archive (multi-GB parquet; never commit/push — local data only)
/market-data/

# next.js
/.next/
/.next-e2e/
/.next-*/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files
.env*
!.env.example

# claude code local settings
.claude/

# vercel
.vercel

# typescript
*.tsbuildinfo

# generated assets
public/sqljs/

# browser extension build output + signing key material (key.pem covered by *.pem)
extension/dist/
extension/.key.json
# packaged extension zips (Web Store upload artifacts — built, not source)
extension/*.zip

# scratch screenshots / captures at the repo root (never source)
/*.png
/*.jpg
/*.webm

.vercel

# local-only e2e helper scripts (verification scratchpads)
scripts/diag*.mjs
# transient demo-recording artifacts (auth.json holds a session token!)
demo/auth.json
demo/*.webm
demo/*.wav
demo/*.pcm
demo/*.jpg
demo/clicks.json

# Personal Claude Code settings (machine-local permissions)
.claude/settings.local.json

# Playwright MCP browser artifacts
.playwright-mcp/
