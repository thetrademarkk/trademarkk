# dependencies
/node_modules

# market data archive (multi-GB parquet; never commit/push — local data only)
/market-data/

# next.js
/.next/
/.next-e2e/
/.next-*/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files
.env*
!.env.example

# claude code local settings
.claude/

# vercel
.vercel

# typescript
*.tsbuildinfo

# generated assets
public/sqljs/

# browser extension build output + signing key material (key.pem covered by *.pem)
extension/dist/
extension/.key.json
# packaged extension zips (Web Store upload artifacts — built, not source)
extension/*.zip

# scratch screenshots / captures at the repo root (never source)
/*.png
/*.jpg
/*.webm

# local-only e2e helper scripts + one-off scratch scripts (never commit)
scripts/diag*.mjs
scripts/_*.mjs
scripts/_*.ts

# transient demo-recording artifacts (auth.json holds a session token!)
demo/auth.json
demo/*.webm
demo/*.wav
demo/*.pcm
demo/*.jpg
demo/clicks.json

# Playwright MCP browser artifacts (snapshots/screenshots from local verification)
.playwright-mcp/
/public/duckdb/

# python (ETL scripts)
__pycache__/
*.pyc
.groww_token.json
