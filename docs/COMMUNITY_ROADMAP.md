# Community Roadmap — North Star: LinkedIn/Twitter-grade UX

> **Goal:** continuously evolve the TradeMarkk community until its polish, depth
> and stickiness match the best social products — while keeping the journal's
> privacy-first architecture (journals never leave the user's DB).
>
> This file is the working backlog for the autonomous improvement loop. Each
> iteration: pick the top unchecked item → research the best-in-class pattern →
> plan → build on a `feature/` branch → verify with Playwright (desktop + 360px
> mobile audit) → PR → CI → merge (never delete branches) → confirm auto-deploy
> → check the item off here with a date + PR number. Small, shippable slices —
> one coherent item per iteration.

## Working rules (non-negotiable)

- Branch → PR → CI green → squash-merge; **never** delete branches; **never** deploy manually (merge auto-deploys).
- No emojis in UI — lucide-react icons only. Paise-precise money. No native confirm/alert — in-app dialogs.
- Every interactive surface: instant cache updates (no reloads), a11y labels, mobile-safe (run `scripts/mobile-audit.mjs`).
- Clean up any e2e users (`e2e-%@example.com`) from the platform DB after verification.
- Real users exist now — migrations must be additive/idempotent; never wipe or rewrite user content.

## Backlog (ordered)

Research-driven ranked queue. Already-shipped foundations (DM v1 PR #12, inline
composer PR #18, post-detail polish PR #28, notification grouping PR #36,
profile polish, header search v2) live in the "Shipped by the loop" log below.

1. [x] **Richer reactions** — Like / Insightful / Respect / Celebrate (LinkedIn-style), one per user, hover/long-press picker, stacked summary, reaction-weighted Top-feed. _(2026-06-13, PR — this iteration)_
2. [x] **@mention + $cashtag + #hashtag composer autocomplete** — inline typeahead in the composer & comment box; resolves handles/symbols/tags as you type. _(2026-06-13, PR #63)_
3. [x] **Edit posts/comments in a 15-min window** with an immutable edit history ("edited" marker + revision log). _(2026-06-13, PR #70)_
4. [x] **Decayed, cost-weighted Top-feed hot-score** — comments/reshares weigh more than reactions, recency decay, per-author diversity cap; deterministic, no ML. _(2026-06-13, PR #75)_
5. [x] **Link OG unfurl preview cards** — SSRF-safe host allowlist for fetched links + own OG tags on posts. _(2026-06-13, PR #75)_
6. [x] **$cashtag tagging + per-symbol stream pages** — NSE/BSE symbol master, `post_symbols` join, per-ticker page, not-advice banner, SEO. _[KILLER]_ _(2026-06-13, PR — this iteration)_
7. [x] **Quote post / reshare** + reshare-with-thoughts. _(2026-06-13, PR — this iteration)_
8. [x] **Topic/tag pages + follow-a-tag** — followed tags flow into the Following feed. _(2026-06-14 — accumulated, pending batch deploy)_
9. [x] **Trending tickers & topics board** — computed on-read (no cron), distinct-author gate + recency weighting; "not a recommendation". _(2026-06-14 — accumulated, pending batch deploy)_
10. [x] **Optional Bullish/Bearish sentiment tag** — 24h per-symbol gauge; never a BUY verdict. _[needs #6]_ _(2026-06-14 — accumulated, pending batch deploy)_
11. [x] **Watchlist-driven feed scope** — watched symbol OR followed author. _[needs #6]_ _(2026-06-14 — accumulated, pending batch deploy)_
12. [x] **For-You interest feed + cold-start starter follows** — engaged tags + 2nd-degree by hot-score; onboarding seeds; no ML. _(2026-06-14 — accumulated, pending batch deploy)_
13. [x] **Content-quality + anti-tip/spam gate** — SEBI-sane, conservative pure heuristics: anti-tip/pump nudge (soft-flags tip/call/assured-return language for moderation review; HARD-blocks only egregious paid-group/solicitation spam), low-effort/all-caps/link-only/keyboard-mash gate, near-duplicate repost gate. Flagged posts surface to the admin queue (feeds rank-14). _(2026-06-14 — accumulated, pending batch deploy)_
14. [x] **Admin moderation / report-queue UI** — unified admin-only queue merging user **reports** + **auto-flagged** posts (rank-13 quality gate) in one filterable (source · open/actioned), sortable, paginated list; moderator actions: dismiss (mark report actioned, kept as history), delete content (post/comment cascade), clear a false-positive quality flag, and **suspend/reinstate a user** (additive `user.status='banned'` blocks posting/commenting/resharing/editing at the create endpoints with a 403; existing content stays); append-only `mod_actions` audit log (actor/action/target/at). Strictly admin-only (isAdmin + isAllowedOrigin + zod + rate-limit; non-admins 403 on every endpoint). _(2026-06-14 — accumulated, pending batch deploy)_

15. [x] **"N new posts" live pill** — a floating, accessible "N new posts" pill above the Latest feed that counts posts newer than the post the viewer is currently seeing at the top and, on click, loads them in at the head + scrolls there. **Transport = a cheap count-only POLL, not SSE** (decision: the repo has no SSE/EventSource anywhere and every live surface already polls via TanStack Query; Vercel serverless is hostile to long-lived SSE — documented in the code + commit). New `GET /api/community/posts/new-count?since=` returns ONLY a count (no payloads until click), reuses the feed's shared `buildFeedConditions` so it honors the exact same visibility rules (block-aware, excludes the viewer's own brand-new posts, honors tag/symbol/scope filters). The `useNewPostsCount` hook polls every 25s with `refetchIntervalInBackground:false` (pauses while the tab is hidden) + `refetchOnWindowFocus` (one fresh check on return) — no thundering herd. Gated to the live recency-ordered Latest global view only (off on Top/Saved/Following/Watchlist/For-You and tag/symbol/search streams). Purely additive client behaviour — does not disturb the SSR-seeded ISR first page. _(2026-06-14 — accumulated, pending batch deploy)_

**Later (16–20):** reputation/track-record · follow suggestions · event/earnings threads · notification preferences · awards · then DM v2 (images/typing/read-receipts) · muted words.

## Shipped by the loop

<!-- The loop appends: - [x] YYYY-MM-DD — item — PR #N -->

- [x] 2026-06-14 — "N new posts" live pill (rank-15) — count-only POLL transport (NOT SSE — no EventSource in the repo, Vercel-serverless-friendly, matches the existing notification/DM polling); new `GET /api/community/posts/new-count?since=` reuses the feed's shared `buildFeedConditions` (block-aware, excludes the viewer's own posts, honors tag/symbol/scope); `useNewPostsCount` polls 25s, pauses on hidden tab (`refetchIntervalInBackground:false`) + refetches on focus; accessible floating pill (lucide ArrowUp, aria-live polite) gated to the live Latest scope only, click prepends new posts + scrolls to top; feed dedupes by id to avoid refetch boundary dups — (accumulated, pending batch deploy)
- [x] 2026-06-14 — Admin moderation / report-queue UI (rank-14) — unified reports + auto-flagged moderation queue, dismiss/delete/clear-flag/ban-user actions, additive `user.status` ban flag + `mod_actions` audit log, admin-only — (accumulated, pending batch deploy)

- [x] 2026-06-12 — Direct messages (chat) v1 — PR #12
- [x] 2026-06-12 — Feed composer upgrade (inline top-of-feed composer, drafts in tm.community-draft) — PR #18
- [x] 2026-06-12 — Post detail polish (back-nav context, share counts, related rail, docked mobile composer, follow chip) — PR #28
- [x] 2026-06-12 — Notification grouping (LinkedIn-style rollups, scoped mark-read, /community/notifications page) — PR #36
- [x] 2026-06-13 — Richer reactions (Like/Insightful/Respect/Celebrate; additive `likes.reaction` + denormalized `posts.reactions`; hover/long-press picker, stacked summary, weighted Top-feed) — PR (this iteration)
- [x] 2026-06-13 — @mention + $cashtag + #hashtag composer autocomplete (caret-aware typeahead in the composer & comment box; block-aware @users + #tags via `GET /api/community/autocomplete`, curated in-repo $symbols client-side w/ free entry; keyboard-navigable listbox; rich-text linkifies all three; signed-in 360px header overflow fixed) — PR #63
- [x] 2026-06-13 — Edit posts/comments in a 15-minute window + immutable edit history (author-only `PATCH /api/community/posts/[id]` & `/comments/[id]`, server-enforced window → 410 once closed, same zod re-validation + newly-added @mention re-extraction as create; additive `edited_at` + append-only `edit_history` JSON columns on posts & comments; owner "Edit" action with "N min left" hint, inline form reusing the composer, "Edited · view history" marker + read-only history dialog; optimistic edit with rollback; edits rate-limited 30/h) — PR #70
- [x] 2026-06-14 — Topic/tag pages + follow-a-tag (dedicated `/community/t/[tag]` page with #tag header, post count, Latest/Top tabs reusing the block-aware tag feed filter, not-advice banner, SEO metadata/OG/canonical, on-demand ISR; additive idempotent `followed_tags` table (user_id, tag) → Following feed now surfaces posts by a followed user OR carrying a followed tag, deduped, block-aware; optimistic Follow button + left-rail "Followed tags" list; tag chips in rich-text/post-cards/topics-rail/search now route to the dedicated page, the `?tag=` feed filter still works) — accumulated, pending batch deploy
- [x] 2026-06-14 — Trending tickers & topics board (rank-9): a /community/trending page + a compact right-rail Trending widget surfacing what the community is actively discussing, WITHOUT being a buy/sell signal. Pure src/features/community/trending.ts ranks (from the post_symbols join) and #topics (from each post JSON tags) by a spam-resistant score: a key only trends once at least 2 DISTINCT authors have posted about it in the window (so one prolific poster spamming a ticker 10x can NEVER make it trend), then distinct authors are the PRIMARY rank key (volume can never overpower author breadth), with a 24h half-life recency-weighted volume + posts + alphabetical tie-breaks. 24h / 7d window toggle. Computed ON-READ from the DB (no cron, no snapshot table): the anonymous board is wrapped in cached() (10-min TTL) + CDN s-maxage, a signed-in viewer board is block-aware + computed per request. Prominent "Reflects community discussion volume, not a recommendation or tip" disclaimer; "Not enough activity yet" empty state; lucide icons, 360px clean. Rows link to /community/s/[symbol] and /community/t/[tag]. GET /api/community/trending?window= — accumulated, pending batch deploy
- [x] 2026-06-14 — Watchlist-driven feed scope (rank-11): a new **Watchlist** feed scope = posts that TAG a watched symbol OR are BY a followed author — the symbol-axis mirror of the rank-8 Following union. Additive idempotent `watched_symbols` table (user_id, symbol PK; indexed by user + by symbol; migrate:platform ran on the platform DB). `queryFeed` gains a `watchlist` scope: one row per post via a correlated EXISTS over `post_symbols` joined to `watched_symbols` OR the followed-author IN subquery (no JS union/dedupe), still block-aware; an empty watchlist degrades gracefully to the Following set (never to nothing). Pure src/features/community/watchlist.ts (normalizeWatchSymbol mirroring the $cashtag grammar, planWatchToggle, toggleWatchedSymbol capped at 50 + sorted, isWatched, dedupePostsById). Watch/Unwatch toggle (lucide Eye/EyeOff, no emoji) on /community/s/[symbol] — optimistic with rollback, signed-out prompts sign-in, dedicated `watch:` rate-limit 30/h. New "Watchlist" feed tab (signed-in only) + left-rail "Your watchlist" mini-list with inline unwatch (×) doubling as a manage view. GET /api/community/watchlist (list) + POST /api/community/watchlist/[symbol] (toggle). `purgeUserContent` clears watched_symbols on account delete. — accumulated, pending batch deploy
- [x] 2026-06-14 — Optional Bullish/Bearish sentiment tag (rank-10): an OPTIONAL, honest community-sentiment lean — explicitly NEVER a buy/sell recommendation. Additive idempotent `posts.sentiment` TEXT (NULL | 'bull' | 'bear'); only persisted when the post mentions >= 1 $cashtag (server re-gates on create AND edit — removing every ticker also clears the lean). Composer + edit-form gain an optional Bullish/Bearish toggle (lucide TrendingUp/TrendingDown, no emoji) that disables itself until a $ticker is in the body and is editable within the existing 15-min edit window; a small bull/bear chip rides the post card's "mentioned tickers" row. Per-symbol page (/community/s/[symbol]) gets a 24h/7d community-sentiment gauge = % bullish vs bearish among recent posts that tagged the symbol AND set a lean, with counts and a prominent "Community sentiment from posts — NOT advice or a recommendation" disclaimer; below a min sample of 3 sentiment-bearing posts it reads "not enough signal". Pure src/features/community/sentiment.ts (computeSentimentGauge percentages summing to 100 + min-sample gate, window helpers). Server querySymbolSentiment computed ON-READ (no cron): block-aware per request for signed-in viewers, the anonymous gauge wrapped in cached() (10-min TTL) + CDN s-maxage. GET /api/community/sentiment?symbol=&window= — accumulated, pending batch deploy
- [x] 2026-06-13 — $cashtag tagging + per-symbol stream pages (the KILLER): expanded `symbols.ts` to a 318-entry NSE/BSE master `{symbol,name,exchange}` + `normalizeSymbol`/`lookupSymbol`/`isKnownSymbol`; pure `cashtags.ts` (`extractCashtags` word-initial `$SYMBOL`, dedupe/uppercase/cap-12, known + free-entry; `planSymbolSync`add/remove plan); additive idempotent`post_symbols`join (PK post_id+symbol, indexed by symbol & post) synced on post create AND edit via`syncPostSymbols`; per-symbol stream at `/community/s/[symbol]`(server header with symbol/company/exchange + post count + "Educational discussion, not investment advice" banner, Latest/Top tabs reusing`queryFeed`filtered by`post_symbols`, block-aware, empty state, composer pre-titled with the ticker; on-demand ISR `revalidate=300`+`generateStaticParams()=>[]`, per-symbol metadata/OG/canonical); rich-text $CASHTAG → `/community/s/SYMBOL` chip + a "mentioned tickers" row on each post card) — PR
- [x] 2026-06-13 — Decayed, cost-weighted Top-feed hot-score finalized + per-author diversity cap (verified the PR #60 `topFeedScore`: weighted reactions Insightful/Respect ×1.5 / Celebrate ×1.2 / Like ×1, comments ×2, Hacker-News gravity decay `(engagement+1)/(age+2)^1.2`; added pure `applyDiversityCap` so at most 2 posts per author lead the Top window — overflow appended, never dropped, deterministic; wired into `queryFeed` over the full scored candidate set before slicing, skipped on single-author profile views) — PR #75
- [x] 2026-06-13 — Link OG unfurl preview cards (rich title/description/image/site card below a post for the FIRST link in its body; new SSRF-safe server fetcher: https-only, DNS-resolves the host + blocks every private/loopback/link-local/ULA/metadata range incl. IPv4-mapped IPv6, manual redirect-following that re-validates each hop so a public URL can't 30x into a private host, 5s timeout, ~512KB read cap, plain-text-only sanitized extraction, optional `UNFURL_ALLOWED_HOSTS` allowlist; additive idempotent `link_unfurls` cache table keyed by URL hash with TTL refresh + negative caching; lazy `GET /api/community/unfurl?postId=` resolving the link server-side, never from the query string; `UnfurlCard` via next/image — browser loads `/_next/image` same-origin so the strict img-src CSP holds — with a lucide Globe fallback, opens in a new tab rel=noopener) — PR #75
- [x] 2026-06-13 — Quote post / reshare + reshare-with-thoughts (rank-7): a reshare/quote is a NEW post whose additive idempotent `posts.quote_post_id` references the ROOT original (`posts.reshare_count` is the original's denormalized tally). Server `createReshare` reuses the post row, is block-aware BOTH ways, allows resharing your own post, collapses a reshare-of-a-reshare to the root so chains never nest, increments the root's `reshare_count`, and notifies the original author with a new `reshare` notification type (folds into the existing grouped notifications); `deletePostCascade` decrements + `purgeUserContent` decrements & clears the user's `post_symbols`. `hydratePosts` embeds the original (author + snippet + trade-card) as a `QuotedPostView`, guarding deleted originals ("post unavailable" card) and viewer-blocked authors (suppressed). UI: a Reshare action on the post card with a menu → "Reshare" (instant) or "Quote" (composer dialog showing the original embedded as a preview); feed/detail render the embedded original card (click → original); reshare count + optimistic increment with rollback; dedicated 30/h `reshare:` rate limit. `POST /api/community/posts/[id]/reshare`. lucide Repeat2, no emojis, 360px clean — PR
- [x] 2026-06-14 — For-You interest feed + cold-start starter follows (rank-12): an optional **For You** first tab (signed-in only; Latest stays the DEFAULT so the feed never silently becomes a filter bubble) that re-ranks recent posts by a transparent, EXPLAINABLE interest score (NO ML, NO new schema — all signals derive from existing tables). Pure `src/features/community/foryou.ts`: `buildInterestProfile` folds the viewer's signals into weighted maps with documented precedence — followed tags (×3) / watched symbols (×3) outweigh tags/symbols merely ENGAGED with (×1, from posts liked/bookmarked/authored); 1st-degree followed authors (×2.5) outweigh 2nd-degree (×1, authors followed by those you follow); a key never double-counts (MAX, not sum). `scoreCandidate` = Σ matched-tag weights + Σ matched-symbol weights + author weight + `HOT_PRIOR_WEIGHT × topFeedScore` (the SAME recency-decayed Top-feed hot-score as the global prior, so the feed is never empty and a real interest match outranks a merely-hot post); `rankForYou` sorts (score → hotScore → id) then applies a per-author diversity cap. Server `queryForYou` pulls a capped recent candidate window (block-aware, excludes the viewer's own posts + empty reshares), scores it, hydrates; a viewer with NO signals (`isColdStart`) falls back to the global Top feed. Cold-start: pure `starter-suggestions.ts` + server `getStarterSuggestions` derive seed tags (from the trending board + curated `SUGGESTED_TAGS`) and popular authors (from a contributor-leaderboard query), excluding what the viewer already follows/is; a "Get started" card (one-tap optimistic follow for tags + traders) shows above the For-You feed only for a thin social graph (`shouldShowStarter`). `GET /api/community/foryou` + `GET /api/community/suggestions`. lucide icons (Sparkles/UserPlus/Plus/Check), no emoji, 360px clean — accumulated, pending batch deploy
- [x] 2026-06-14 — Content-quality + anti-tip/spam gate (rank-13): SEBI-sane, conservative, fully unit-tested pure heuristics in src/features/community/quality.ts. (1) Anti-tip/pump nudge — SOFT-flags tip/call/assured-return language ("BUY X TARGET n SL n", sure-shot/jackpot/multibagger/guaranteed/X% profit) by setting an additive nullable posts.quality_flag ("tip") for moderation review + a NON-blocking composer warning; HARD-blocks ONLY egregious paid-group/solicitation spam (join-my-paid-telegram, DM-me-for-calls, bare t.me/wa.me invites). A genuine-analysis downgrade keeps reflective posts ("I stopped following intraday tips…") clean. (2) Low-effort/spam gate — server-side rejects empty/near-empty, all-caps walls (soft-flag), keyboard-mashing, and link-only/link-dominated posts. (3) Near-duplicate gate — blocks a user re-posting near-identical text (Jaccard ≥0.85 over a 24h window; exact-match for short bodies). evaluatePostQuality wired into POST + PATCH (edit re-runs the gate, near-dup excludes the post itself, edit re-evaluates the flag). Flagged posts surface to the admin report queue (new flagged[] in GET /api/admin/reports + an "Auto-flagged by the quality gate" section) so rank-14 can consume them. Additive idempotent posts.quality_flag column + idx_posts_quality_flag (migrate:platform ran). 53 new vitest incl. genuine-analysis FALSE-positive guards. lucide AlertTriangle, no emoji, 360px clean — accumulated, pending batch deploy
