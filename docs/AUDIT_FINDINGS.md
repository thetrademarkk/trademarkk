Wrote the synthesized, prioritized audit report to `C:\Users\raash\Desktop\trading-journal\docs\AUDIT_FINDINGS.md`.

Summary of what it contains:
- **Executive summary** with severity counts (1 P0, 13 P1, 8 P2, 6 P3 = 28) and the top 5 must-fix-now.
- **Deduplication:** the two SSRF/DNS-rebinding findings (sec-platform P1 + sec-dm P2) were merged into a single P1 entry (#2), since they share the same root cause at `src/server/unfurl.ts:117`. This is why the total is 28 and not 29 raw findings, and is called out explicitly under the summary table.
- **Grouped by severity → dimension.** Each finding has a clear title, exact `path:line`, impact, and concrete fix.
- **Honesty preserved from the verdicts:** where reviewers refuted or narrowed a sub-claim, I carried that into the impact text rather than restating the original framing — e.g. the journal-invalidation "community network refetch storm" (#15) is noted as not occurring; the seg-lots "documented hang" (#21) is framed as test-infra robustness with the `isVisible` mis-flag corrected; the SEO profile bug (#8) notes the `post/[id]` canonical is also affected.

One note: the final confirmed finding (P3 backtest run-result blob — included as #22 at P2 per its `severity` field) had its `recommendation` truncated mid-sentence in the input JSON ("...enforce a hard request-body size limit in POST /api/backtest/runs by"). I completed the fix guidance from the finding's own detail (reject oversized payloads before parsing) rather than inventing new specifics.

---

## Fix status (2026-06-14) — branch `fix/audit-all`

All audit lanes were consolidated into `fix/audit-all` (correctness CORR-01..06,
SEO-01..08, PWA-01..09, horizon, SSRF, web-vitals, the P0 e2e fix, governance/UI)
and the remaining pending findings were fixed in five verified batches. Gates after
every commit: `tsc`, `next lint` (0 warnings), `ext:typecheck`, `vitest` (2501),
`next build`.

**Fixed in the consolidation** (prior lanes): P0 e2e-smoke; SSRF DNS-rebinding;
payoff max profit/loss (CORR-01/06); agri charge classifier (CORR-02); IST
bucketing (CORR-03/04); calendar expiry edge (CORR-05); profile/leaderboard
self-canonical + sitemap blog + community h1 (SEO-01/02/04/05); post-detail
canonical (SEO-08); messages/notifications/reset-password noindex (SEO-05/06);
blog TipTap split + feed-image CLS (web-vitals); offline copy + manifest PNG +
build-derived SW version (PWA-01/03/05).

**Batch A — security/authz** (`a6947d0`): #7 report-target validation, #8
block-stops-follow/notify, #28 banned comment-edit gate, #29 feed LIKE escape,
#30 origin Sec-Fetch-Site, #31 client-ip trusted hop, #32 starter banned/block
exclusion, #33 discovery rate limits, #34 RunResult size bound, #35 token TTL 24h.

**Batch B — money/charges/tax** (`bbdd5a4`): #16 per-leg brokerage cap, #17
COMM/CDS in turnover, #39 same-day-CNC speculative, #40 gross-basis turnover,
#20 per-leg recompute option_type, #1 NCDEX exchange propagation (app), #21
symbol-prefix trim, #44 day-first CSV dates.

**Batch C — perf** (`a7d6c27`): #3/#9/#11 covering indexes, #5 useThread poll
storm, #15 DM inbox 30s, #4 scoped invalidations, #38 dashboard/calendar memo,
#13 debounced sql.js persist (+flush durability). #14 (`0a866d3`) tag-JOIN gate.

**Batch D — a11y/SEO** (`8ed10b3`): #22/#23 chart accessible names + reduced
motion, #41 rHistogram label, #24 tag-picker, #50/#51/#52/#53/#54 icon-button
labels, #55 sentiment-gauge, #56 Sheet description, #46 marketing og:url.

**Batch E — engine/extension** (`67b03cc`): #42 coverage accounting, #43 expiry
source, #19 reject underlying-basis triggers, #18 montecarlo empty-sample guard,
#2 extension NCDEX adapters, #48 optimizePackageImports.

**Deferred (recommended follow-up PRs):** #10 normalized `post_tags` table (large
refactor), #25 mobile nav hamburger (UI, needs visual verification), #49 A2HS
install prompt (feature), #48 lazy-mount HeroShowcase (CLS-sensitive), #36/#37
small community-query caps, #6/#26/#27/#60 e2e coverage (seg-lots watchdog,
social funnel, mode-switch, terms/offline), Twitter @handle (needs a real handle).
The 480-finding `AUDIT_A11Y_SEO_ATTRIBUTES_2026-06-14.md` BATCH 0–8 plan is a
separate effort, not started here.

---

## Appendix — all findings

### P0 (1)

- **e2e-smoke.mjs is syntactically broken — `npm run e2e` cannot run at all** — `scripts/e2e-smoke.mjs:130-141` _(e2e-coverage, high)_
  - The canonical smoke suite (the only spec wired into package.json `"e2e": "node scripts/e2e-smoke.mjs"`) fails `node --check` with `SyntaxError: Unexpected end of input` at line 459. The `await step("/app/backtesting 308-redirects to /backtesting", async () => { ... })` callback opened at line 130 is never closed: line 140 ends with `if (resp && resp.status() >= 400) throw new Error(...)` and line 141 immediately starts a NEW `await step("For-You + starter-suggestions endpoints respond", ...)` *i
  - **Fix:** Insert the missing `});` after line 140 (close the backtesting-redirect step before the For-You step begins). Then run `node --check scripts/e2e-smoke.mjs` to confirm it parses, and execute it against a prod build to confirm the spliced-in steps actually pass. Add a fast `node --check scripts/e2e-*.mjs` lint gate to CI (no browser needed) so a parse break in any spec fails the build immediately — 


### P1 (12)

- **DNS-rebinding TOCTOU bypasses the unfurl SSRF guard (metadata endpoint reachable)** — `src/server/unfurl.ts:117` _(sec-platform, high)_
  - assertSafeUrl (src/server/ssrf.ts:188) validates safety by calling dns.lookup(host) and rejecting if ANY resolved address is private. But fetchUnfurl then connects with fetch(safe.url.toString()) using the HOSTNAME (unfurl.ts:117), and Node/undici performs its OWN independent DNS resolution at connect time. The address that is validated and the address that is actually connected to are resolved separately, so an attacker who controls authoritative DNS for their domain with a low TTL can return a
  - **Fix:** Pin the validated IP into the actual connection. Resolve once in assertSafeUrl, filter to safe addresses, and connect to that exact IP while preserving the Host header + TLS SNI: pass a custom undici Agent with a `connect: { lookup }` (or `dns.lookup`) hook that returns ONLY the pre-validated address(es), or rewrite the URL to the literal IP and set the Host header. Apply the same pinning on every

- **Net-long-put books report maxProfit = Infinity / "Unlimited" (a long put's profit is bounded at S=0)** — `src/lib/options/payoff.ts:192` _(logic-options-backtest, high)_
  - buildPayoffCurve computes downGain = puts > 0 and then sets maxProfit: (profitUnbounded || downGain) ? Infinity and profitUnbounded: (profitUnbounded || downGain). A net-long-put position (e.g. a bare Long Put, a long put butterfly/backspread with net long puts) is NOT unbounded above: as S -> 0 the put profit maxes out at qty*(strike - premium), a FINITE value (the underlying cannot go below 0). The inline comment even acknowledges "the loss is bounded because S is floored at 0" yet still flips
  - **Fix:** Do not treat net long puts as unbounded profit. The true max profit of the put-tail is realized at S=0 and is finite, so simply DROP the downGain branch (and add S=0 / a near-0 sample point to the curve so the sampled maxProfit captures the S->0 corner). Only net long CALLS (calls > 0) are truly unbounded above. Add a unit test for Long Put expecting maxProfit = qty*(strike - premium) and profitUn

- **maxLoss is understated for short-put / net-downside books because the sampled price range never reaches S=0** — `src/lib/options/payoff.ts:103-114` _(logic-options-backtest, high)_
  - payoffRange centers on the strikes and pads by max(spread, center*0.2, 1); lo = Math.max(0, min(minK, center - pad)). For a single strike this floors lo at ~80% of the strike, never near 0. buildPayoffCurve then computes maxLoss as the minimum P&L over only the SAMPLED points (payoff.ts:158-163), and maxLoss is reported as that sampled min unless lossUnbounded (which is only true for net short CALLS). A bare Short Put (or the put leg of a short strangle) has its worst case at S=0 = qty*(premium 
  - **Fix:** Always include S=0 (and optionally a point just above the lowest strike) in the sample set so the piecewise-linear minimum captures the S->0 corner for put-heavy books; or compute the closed-form put-tail loss analytically. Add a test for Short Put expecting maxLoss = qty*(premium - strike).

- **Two divergent agri-commodity classifiers disagree; the one driving STORED charges wrongly exempts processed commodities (GUARGUM, oilcakes) from CTT** — `src/features/trades/instrument-parse.ts:105-149` _(logic-trades-segments, high)_
  - There are two independent agri classifiers that are used in different money paths and disagree on at least 7 NCDEX commodity bases:

(1) parseContractName(...).agri — used by the FORM (utils.ts:47), CSV import (csv.ts:188,232) and recompute (recompute.ts:83) to set the charge engine's agriCommodity flag. It derives agri via isAgriBase = isNcdexAgriBase(prefix-match over NCDEX_AGRI_BASES) || MCX_AGRI_BASES. NCDEX_AGRI_BASES INCLUDES 'GUARGUM', 'COCUD', 'COTTONSEEDOILCAKE', 'KAPASKHALI', 'JEERAUNJ
  - **Fix:** Make the charge-path agri flag use the same SEBI Rule-3 logic as classifyAgriCommodity (i.e. honour AGRI_PROCESSED_EXCEPTIONS). Either have parseContractName's agri derivation call classifyAgriCommodity for COMM symbols, or remove the processed entries (GUARGUM, COCUD, COTTONSEEDOILCAKE, KAPASKHALI) from the agri side of the parser and add the same processed-exception screen. One source of truth f

- **NCDEX commodities never get the NCDEX exchange transaction rate — both form and CSV store MCX/NSE, undercharging exchange fees up to ~3x** — `src/features/trades/csv.ts:215` _(logic-trades-segments, high)_
  - The charge config defines distinct NCDEX exchange transaction rates (brokers.ts:152-161): commodityFuture 0.0058% and commodityFutureAgri 0.003%, vs MCX's uniform 0.0021%. resolveExchange/normalizeExchange/EXCHANGE_FILTERS and the NCDEX: / NCD: / NCO: parser prefixes all support NCDEX. BUT no persistence path ever stores exchange='NCDEX':
- CSV import: buildTrade calls resolveExchange(inst.segment) (csv.ts:215), which returns MCX for any COMM. parseContractName strips the 'NCDEX:' prefix and dis
  - **Fix:** Propagate the parsed exchange. parseContractName should expose the resolved exchange (NCDEX for NCDEX:/NCD:/NCO: prefixes and for NCDEX-only agri bases), and buildTrade should store it instead of resolveExchange(segment). Add an exchange selector (or at least NSE/MCX/NCDEX inference) to the trade form rather than hardcoding 'NSE' in save-statements.ts:34.

- **NCDEX commodities mis-classified as equity — broker adapters drop the real "NCDEX" exchange code (wrong charges)** — `extension/src/brokers/kite.ts:22` _(logic-community-extension, high)_
  - EXCHANGE_TOKENS (kite.ts:22-31) — the canonical set reused by ALL five adapters — lists the truncated token "NCD" but NOT the real exchange code "NCDEX" that Indian brokers actually display for the National Commodity & Derivatives Exchange. Consequences, verified end-to-end:

1. Exchange dropped: normalizeGrowwExchange/normalizeDhanExchange/normalizeUpstoxExchange/normalizeFyersExchange split the tag on [^A-Z]+ and test EXCHANGE_TOKENS.has(token); for real text "NCDEX" (or "NCDEX • Commodity") t
  - **Fix:** Add "NCDEX" to EXCHANGE_TOKENS in kite.ts (alongside or instead of "NCD"; keep "NCD" only if a broker truly emits it). Mirror this in the four leading-prefix-strip regexes (upstox.ts:55, groww.ts:62, dhan.ts:68, fyers.ts:71 — and fyers.ts:44 FYERS_KEY) so they read ^(?:NSE|BSE|NFO|BFO|MCX|CDS|NCDEX|NCD|NCO|BCD). Update tests to use the real "NCDEX" exchange text and assert CapturedOrder.exchange =

- **User profile pages inherit canonical "/community" — they self-deduplicate out of the index** — `src/app/community/u/[username]/page.tsx:1-13` _(seo, high)_
  - The profile route's server page (page.tsx) has no generateMetadata. In the App Router, metadata declared in a layout inherits to child pages that don't override it. The community layout sets `title: { default: "Community" }` and `alternates: { canonical: "/community" }` (src/app/community/layout.tsx:10-15). Therefore EVERY public profile page (/community/u/<username>) emits <link rel="canonical" href=".../community"> and <title>Community</title>. Profile pages are public, shareable (linked from 
  - **Fix:** Add an async generateMetadata to src/app/community/u/[username]/page.tsx that resolves the profile (displayName) and returns `{ title: <displayName>, description, alternates: { canonical: `/community/u/${username}` }, openGraph: {...} }`, mirroring the pattern in s/[symbol]/page.tsx:19-39 and post/[id]/page.tsx:20-48.

- **Blog article route bundles the entire TipTap/ProseMirror editor via RichContent** — `src/app/(marketing)/blog/[slug]/page.tsx:7` _(web-vitals, high)_
  - The public, SEO-targeted, ISR blog article page statically imports RichContent from src/components/ui/rich-editor.tsx (line 7, used at line 158). That module is a single "use client" file that also defines RichEditor, which statically imports the full TipTap stack at the top of the file: @tiptap/react (useEditor, EditorContent), @tiptap/starter-kit, @tiptap/extension-link, @tiptap/extension-image, @tiptap/extension-placeholder, plus compressImage and ~13 lucide icons (rich-editor.tsx:4-25). Beca
  - **Fix:** Move RichContent into its own module (e.g. src/components/ui/rich-content.tsx) that has no TipTap imports — it only needs cn + the PROSE_CLASS string and dangerouslySetInnerHTML, so it can even be a server component (drop "use client"). Update the blog [slug] page and admin section to import from the new module. The article route then ships zero TipTap JS, and RichEditor stays lazy-loaded only on 

- **Local/BYOD journal does NOT work offline — "privacy-first, no-network" promise is aspirational, not real** — `public/sw.js:30-34` _(pwa-offline, high)_
  - The service worker handles all `request.mode === "navigate"` requests network-first with a fallback to the precached `/offline` page (sw.js:30-34). The app shell HTML for the journal routes (`/app/dashboard`, `/app/trades`, etc.) is never precached and never runtime-cached (the runtime cache-first branch at sw.js:38-54 only matches `/_next/static/`, `/sqljs/`, `/icons/`). So when the device is offline, navigating/opening the installed PWA fetches the page over the network, fails, and is served t
  - **Fix:** For the journal surface, precache an app-shell document and serve it cache-first (or stale-while-revalidate) for `/app/*` navigations instead of network-first→/offline. Either (a) precache the `/app/dashboard` (or a dedicated `/app/shell`) HTML in the install step and respondWith it from cache on navigation failure to any `/app/*` route, or (b) switch app navigations to a cache-first/SWR strategy 

- **sql.js WASM is not precached — local DB silently fails to initialize offline before first online use** — `public/sw.js:3` _(pwa-offline, high)_
  - PRECACHE = ["/offline", "/icons/icon.svg", "/icons/icon-maskable.svg"] (sw.js:3) does NOT include the sql.js WebAssembly binary. createLocalDb() loads it via `locateFile: (f) => /sqljs/${f}` (src/lib/db/adapters/local.ts:152), i.e. it fetches `/sqljs/sql-wasm.wasm`. The SW only caches `/sqljs/*` runtime cache-first (sw.js:38-42), so the WASM is in the cache ONLY after it has been fetched once while online. A user who installs the PWA and then goes offline before ever opening local mode (or after
  - **Fix:** Add the sql.js WASM (both `/sqljs/sql-wasm.wasm` and `/sqljs/sql-wasm-browser.wasm`, since copy-assets.mjs ships both) to the install-time PRECACHE list so local mode can cold-start with no network. Bump VERSION when doing so to force re-precache.

- **CI 'verify' job runs NO Playwright — every e2e spec is local-only, manually-run, per-lane** — `.github/workflows/ci.yml:9-35` _(e2e-coverage, high)_
  - The single CI job `verify` runs typecheck, ext:typecheck, lint, vitest (`npm test`), `next build`, and `ext:build`. There is no step that installs Chromium or runs any of the 50 scripts/e2e-*.mjs specs. Playwright is a devDependency (package.json:103) but is never invoked in CI; the file headers explicitly say so (e.g. e2e-smoke.mjs:6 'Playwright is not a project dependency' / 'local only'). Consequence: the entire e2e layer — auth, trade logging, charges, community, DM, backtest, mode-switching
  - **Fix:** Add a separate CI job (or nightly scheduled workflow) that boots a prod build with the AUTH_TEST_HOOK harness env and a throwaway Turso platform DB, then runs the critical-path specs (smoke, auth, account-settings, seg-recompute, bt-run, community, dm-v2) headless. Keep it off the per-PR critical path if runtime is a concern (use `workflow_dispatch` + nightly `schedule`), but at minimum gate the c

- **No orchestrator / runner — 50 specs each need a different hand-started server on a different port** — `scripts/e2e-smoke.mjs:13` _(e2e-coverage, high)_
  - There is no scripts/run-e2e*.mjs or _setup helper (confirmed: glob for scripts/run-e2e* and scripts/_*.mjs returns nothing). Each spec hard-codes its own BASE_URL default on a distinct port — smoke=3000, auth=3100, account-settings=3200, seg-lots=3500, bt-run=3600 — and several need bespoke build env (AUTH_TEST_HOOK=1, blank RESEND/GOOGLE creds, a live Turso platform DB, or BYOD_URL/BYOD_TOKEN). The only package.json e2e entry runs the (currently broken) smoke spec. This makes full-suite executi
  - **Fix:** Add scripts/run-e2e.mjs that (1) builds once with the test harness env, (2) starts the prod server on a single port, (3) runs each spec sequentially with a per-spec timeout (hard-kill on hang — see seg-lots), and (4) prints an aggregate matrix of pass/fail. Have it accept a filter arg so a lane can still run one spec. Wire it as `npm run e2e:all`.


### P2 (33)

- **Leaderboard contrib query: full-table GROUP BY scans with no time-bounded index (comments.created_at + likes.created_at unindexed)** — `src/app/api/community/leaderboard/route.ts:51-72` _(perf-server-queries, high)_
  - The contrib leaderboard builds three correlated sub-aggregates joined onto EVERY profile: COUNT(*) over posts, over comments, and over likes JOIN posts — each filtered by `created_at >= since` and GROUPed BY user_id, with NO outer LIMIT pushed into the sub-aggregates (the LIMIT 50 only applies after all users are aggregated and sorted). The migration (scripts/migrate-platform.ts) has NO index on comments.created_at and NO index on likes.created_at. So the comments and likes-received sub-aggregat
  - **Fix:** Add `CREATE INDEX idx_comments_created ON comments (created_at)` and `CREATE INDEX idx_likes_created ON likes (created_at)` to migrate-platform.ts so the time-window sub-aggregates are index-range scans. Better: a composite `idx_posts_user_created ON posts (user_id, created_at)` (and same shape for comments) lets each GROUP BY user_id over a window be an index-grouped scan. Consider a denormalized

- **Journal mutations call qc.invalidateQueries() with no key — every save re-runs every cached query (full sql.js scans + community network refetches)** — `src/features/trades/queries.ts:205` _(perf-client, high)_
  - useSaveTrade, useDeleteTrade, useAddAttachment, useDeleteAttachment, useImportTrades and useApplyRecompute all do `onSuccess: () => qc.invalidateQueries()` with NO query filter (lines 205, 221, 247, 258, 304, 360). The same unscoped pattern is in rules/queries.ts:56,70, workflow/queries.ts:24, playbooks-panel.tsx:51,64, tags-section.tsx:64, settings account-section.tsx:29, storage-section.tsx:104, onboarding setup-form.tsx:99,118, and community api.ts:1415 (useUpdateProfile). Unscoped invalidate
  - **Fix:** Scope each invalidation to the keys actually affected, e.g. `qc.invalidateQueries({ queryKey: ['trades'] })` plus ['all-legs'], ['streak'], ['day-trades'] as needed for trade writes; ['rules']/['rule-breaks'] for rule writes; ['tags']/['trades'] for tag writes, etc. Never call invalidateQueries() with no argument — it always cross-invalidates the community network layer and forces full re-scans of

- **useThread invalidates the conversations list on EVERY 5s poll — refetch storm on the messages page** — `src/features/community/api.ts:1169` _(perf-client, high)_
  - useThread's queryFn unconditionally calls `void qc.invalidateQueries({ queryKey: ['community-dms'] })` on every successful fetch. Because the thread polls every 5s (backgroundAwarePoll(5_000), line 1175), the conversations list is invalidated and refetched every 5 seconds for as long as a thread is open. On the messages page (src/app/community/messages/page.tsx:61,67) MessagesInbox is mounted alongside MessageThread and ALREADY polls community-dms every 5s itself (messages-inbox.tsx:12, useConve
  - **Fix:** Only invalidate community-dms when the thread fetch actually changed read state (e.g. compare returned unread/lastReadAt to the previous cache and invalidate conditionally), or drop the per-poll invalidate entirely and rely on the inbox's own poll + the explicit invalidations already done in useSendMessage/useMarkThreadRead. Do not invalidate a sibling polling query inside another query's queryFn.

- **Leaderboard page inherits canonical "/community" and the title "Community"** — `src/app/community/leaderboard/page.tsx:33` _(seo, high)_
  - leaderboard/page.tsx is a "use client" component (line 1) with no metadata export, so like the profile route it inherits the community layout's `title: "Community"` and `alternates.canonical: "/community"` (src/app/community/layout.tsx:10-15). The leaderboard is a distinct, public, indexable page but declares itself a duplicate of the community home and carries the wrong <title>, so it won't rank or appear correctly in SERPs / shares.
  - **Fix:** Split a server page.tsx that exports `metadata` (title "Leaderboard", own description, `alternates: { canonical: "/community/leaderboard" }`, OG) and renders the existing client component, or convert the file to a server page that imports a `"use client"` child. Also add /community/leaderboard (and /community/trending) to the sitemap.

- **Sitemap omits approved community blog posts — published, canonicalized URLs are never submitted** — `src/app/sitemap.ts:28-33` _(seo, high)_
  - sitemap.ts only iterates the static editorial `POSTS` array for /blog/<slug> entries. But the blog index (src/app/(marketing)/blog/page.tsx:18) and article route render approved community submissions too: listBlogPosts() merges editorial + DB-approved posts (src/server/blog-posts.ts:42-71), and each community article is fully indexable with its own canonical and OG (src/app/(marketing)/blog/[slug]/page.tsx:22-42). These community blog URLs are public and linked, yet absent from the sitemap, so d
  - **Fix:** Make sitemap() async and source the blog URLs from listBlogPosts() (or a lightweight slug+date query) instead of the static POSTS import, so approved community articles are included with their reviewedAt lastModified. Keep the editorial fallback for CI builds where the platform DB is unreachable (listBlogPosts already degrades gracefully).

- **e2e-seg-lots (SEG-10) is the documented hang and has no timeout guard — will block any runner** — `scripts/e2e-seg-lots.mjs:91-209` _(e2e-coverage, medium)_
  - The task notes SEG-10's e2e-seg-lots hung; the spec is syntactically valid (passes node --check) so the hang is runtime. It is a single long sequential chain of `await page.getByText(...).waitFor(...)` / `getByLabel(...)` steps against the lots↔units helper (Lots/Lot size fields, '= N qty' readout, '2 lots' badge). Any selector that no longer matches (helper copy change, label rename, combobox reorder) leaves a `waitFor` blocking until its 10s timeout — but several assertions read innerText/inpu
  - **Fix:** Wrap the whole run in a global watchdog (e.g. a setTimeout that calls process.exit(1) after N minutes) as the other long specs lack too, and give every implicit-wait read (innerText/inputValue/isVisible) an explicit timeout or precede it with a `.waitFor({timeout})`. Then re-verify against the current trade-entry DOM — the lots helper UI may have drifted since SEG-10 (the repo is now on a 'horizon

- **Report route accepts arbitrary/non-existent targetId and mismatched targetType (moderation-queue pollution)** — `src/app/api/community/report/route.ts:41-49` _(sec-community-content, high)_
  - POST /api/community/report inserts a report row using parsed.data.targetId / targetType with NO check that the target post/comment actually exists, that the targetType matches the id's real kind, or that the target isn't the reporter's own content. zod only constrains shape (targetType enum, targetId<=40 chars, reason enum). A signed-in user can therefore submit up to 10 reports/hour against fabricated or enumerated IDs (or mis-typed ids, e.g. a comment id reported as a post). queryModQueue (src
  - **Fix:** Before insert, look up the target by (targetType,targetId) and 404 if it doesn't exist; reject self-reports; this also lets you skip writing rows for already-deleted content. e.g. resolve post/comment row first, return 404 when absent, then insert.

- **Blocking does not stop the blocked user from following/notifying the blocker (one-directional block on the social-graph write path)** — `src/app/api/community/users/[username]/follow/route.ts:41-46` _(sec-community-social, medium)_
  - The follow POST only rejects self-follow and missing targets. It never checks whether the *target* has blocked the viewer. So a user A whom B has blocked can still follow B (line 41-45) and `notify({ userId: target.userId, ..., type: "follow" })` (line 46) fires a notification straight to B. `notify()` in src/server/community.ts:134-170 only consults notification-prefs and never checks the blocks table in either direction, so the same hole lets a blocked user reach the blocker via comment/reply/
  - **Fix:** In the follow route, after resolving `target`, reject (e.g. 403) when the target has blocked the viewer: `SELECT 1 FROM blocks WHERE blocker_id = target.userId AND blocked_id = session.user.id`. More robustly, add a reverse-block guard inside `notify()` (skip creating a row when `input.userId` has blocked `input.actorId`) so follow/comment/reply/mention notifications from a blocked user are all su

- **DM/post unfurl SSRF guard is bypassable via DNS rebinding (validate-then-fetch TOCTOU; allowlist off by default)** — `src/server/unfurl.ts:117` _(sec-dm, high)_
  - fetchUnfurl() validates the target with assertSafeUrl() (src/server/ssrf.ts:164), which DNS-resolves the host and rejects private/loopback/link-local/metadata IPs. But the subsequent fetch(safe.url.toString(), ...) at src/server/unfurl.ts:117 passes the HOSTNAME, so Node's undici performs a SECOND, independent DNS resolution at connect time. There is no IP pinning between the check and the connect (no custom dispatcher/lookup, confirmed: no `dispatcher`/`lookup`/`connect` in unfurl.ts). An attac
  - **Fix:** Pin the connection to the exact IP that assertSafeUrl validated: have assertSafeUrl return the resolved safe address, then make fetch connect to that IP while preserving the original Host/SNI — e.g. pass a custom undici Agent/Dispatcher with a `connect` that overrides the lookup to the pre-validated address (or set the URL host to the validated IP and send the original hostname via the Host header

- **Notifications list + unread-count: ORDER BY/WHERE not covered by the (user_id, read, created_at) index column order** — `src/app/api/community/notifications/route.ts:17-32` _(perf-server-queries, high)_
  - The list query filters `user_id = ?` and ORDER BY `created_at DESC`, but the only matching index is idx_notifications_user `(user_id, read, created_at DESC)` (migrate-platform.ts:225). Because `read` sits BETWEEN user_id and created_at in the index, SQLite can seek on user_id but cannot use the index to satisfy the created_at ordering (read is unconstrained in the list query), forcing a scan of all of the user's notifications + a sort. The unread COUNT query (`user_id = ? AND read = 0`) DOES mat
  - **Fix:** Either add `CREATE INDEX idx_notifications_user_created ON notifications (user_id, created_at DESC)` to back the list-ordering path, or reorder the existing index to `(user_id, created_at DESC, read)` — the unread COUNT still benefits from the (user_id) prefix while the common list query gets index-ordered reads. Pick the column order to serve both reads from one index.

- **Feed tag filter and tag-count use `tags LIKE '%"tag"%'` — unindexable full-table scan on the hottest path** — `src/server/community.ts:751-752` _(perf-server-queries, high)_
  - buildFeedConditions filters tag feeds with `posts.tags LIKE '%"tag"%' ESCAPE '\\'` (also countPostsForTag at community.ts:1253-1257). A leading-wildcard LIKE can never use an index, so every tag feed page and every tag-page header count is a full scan of the posts table, only then ordered by created_at and limited. There is no inverted/normalized tag table — tags live as a JSON blob in posts.tags. The trending board (loadTagRows community.ts:1593-1607) and follow-suggestions (community.ts:2001-2
  - **Fix:** Introduce a normalized `post_tags (post_id, tag)` join table (mirroring post_symbols) with `CREATE INDEX idx_post_tags_tag ON post_tags (tag)` and rewrite the tag feed/count to `posts.id IN (SELECT post_id FROM post_tags WHERE tag = ?)` — turning the scan into an indexed lookup. This also speeds trending topics and shared-tag follow suggestions, which currently all json_each-scan posts.

- **recentPostBodiesByUser + reputation per-user post/comment scans lack a (user_id, created_at) composite index** — `src/server/community.ts:421-426` _(perf-server-queries, high)_
  - recentPostBodiesByUser queries `posts WHERE user_id = ? AND created_at >= ? ORDER BY created_at DESC LIMIT 20` (runs on every post create/edit for the near-duplicate gate). The only usable index is idx_posts_user `(user_id)` (migrate-platform.ts:228), which seeks the user but then must filter the createdAt range and SORT the result, since created_at is not in the index. The same pattern recurs in collectReputationSignals: the activeWeeks UNION (community.ts:1819-1825) scans posts and comments by
  - **Fix:** Add `CREATE INDEX idx_posts_user_created ON posts (user_id, created_at DESC)` and `CREATE INDEX idx_comments_user_created ON comments (user_id, created_at DESC)` to migrate-platform.ts. These let the per-user windowed/ordered reads in recentPostBodiesByUser, the activeWeeks UNION, and reputation aggregates run as index-range scans instead of seek+sort.

- **For-You / Top feed candidate scans rely on idx_posts_created but force a full sort under the blocked-author subquery and tag/symbol predicates** — `src/server/community.ts:1084-1093` _(perf-server-queries, medium)_
  - queryForYou pulls up to FORYOU_CANDIDATE_CAP=150 rows with `WHERE created_at >= since AND user_id != ? AND quote_post_id IS NULL AND user_id NOT IN (SELECT blocked_id FROM blocks ...) ORDER BY created_at DESC`. The Top feed (community.ts:824-829) similarly orders by `desc(likeCount + commentCount)` then created_at over a 7-day window. idx_posts_created covers the created_at ordering/range for the Latest/For-You windowed reads (good), but the Top feed's ORDER BY is on a COMPUTED expression `like_
  - **Fix:** This is acceptable for now given the hard candidate caps, but if Top latency grows: precompute a denormalized hot_score column updated on reaction/comment write and add `CREATE INDEX idx_posts_hot ON posts (hot_score DESC, created_at DESC)` so the candidate pre-filter is index-ordered instead of a full-window expression sort. Low urgency because the windows + caps bound it; flagging so the cost is

- **sql.js persists by serializing the ENTIRE database (db.export()) on every write — import does N full serializations** — `src/lib/db/adapters/local.ts:85` _(perf-client, high)_
  - LocalDbClient.persist() calls `this.db.export()` (a full serialization of the whole SQLite file to a Uint8Array) and writes the entire blob to IndexedDB after every execute() (line 112) and every batch() (line 135), all on the main thread. useImportTrades (trades/queries.ts:301) chunks inserts into batches of 100 and awaits db.batch() per chunk, so importing M trades triggers ceil(M/100) full-DB exports — and each export grows as the DB grows, making a large import super-linear in serialization 
  - **Fix:** Debounce/coalesce persistence (e.g. schedule a single export-to-IDB on idle after a burst of writes rather than once per batch), and for bulk import wrap the whole import in one transaction + a single persist at the end. Longer term, move sql.js read/write and export off the main thread into a Web Worker so serialization never blocks the UI.

- **useTrades runs a full trade_tags JOIN scan on every call even when no tag filter is applied** — `src/features/trades/queries.ts:87` _(perf-client, high)_
  - useTrades always calls fetchTagsByTrade(db) (line 87), which does an unfiltered `SELECT ... FROM trade_tags tt JOIN tags g` over the entire tag-link table (lines 29-48), then attaches tags to every trade — regardless of whether filters.tagId is set or whether any caller reads .tags. Most callers (dashboard page.tsx:40, insights page.tsx:47, calendar page.tsx:74, analytics page.tsx:69, goals, playbooks) pass {} or {from,to} and never use the per-trade tags, yet every one of them pays for the full
  - **Fix:** Only fetch/attach tags when needed: gate fetchTagsByTrade behind `filters.tagId` (for the filter) or expose a separate hook for views that render tags, and return trades without the tag-map rebuild otherwise. When filtering by tagId, push the filter into SQL (JOIN trade_tags WHERE tag_id = ?) instead of fetching all tags and filtering in JS.

- **DM inbox list polls every 5s — heavier than the documented 30s policy** — `src/features/community/components/messages-inbox.tsx:12` _(perf-client, high)_
  - MessagesInbox calls useConversations(true, 5_000), polling the full conversations list (with last-message bodies and unread counts) every 5 seconds. The polling design doc states the DM inbox cadence is ~30s (src/features/community/poll.ts:6) and the floating dock uses 30s (messages-button.tsx:18). A 5s list poll is 6x the intended inbox rate; together with the useThread per-poll invalidate (finding above) the conversations endpoint is hit very aggressively whenever the messages page is open. It
  - **Fix:** Raise the inbox poll to the documented ~30s (the open thread already polls at 5s for the active conversation; the inbox only needs to reflect ordering/unread, which 30s + focus refetch covers). If near-real-time inbox ordering is desired, drive it off the thread's own updates rather than a 5s list poll.

- **Brokerage % cap applied to averaged per-order turnover, not each leg's own turnover — over/undercharges lopsided trades** — `src/lib/charges/charges.ts:135-146` _(logic-charges-tax, high)_
  - The percentage-brokerage cap is computed on the AVERAGE per-order turnover: perOrderTurnover = totalTurnover / orders (= (buyTurnover+sellTurnover)/2), then `Math.min(brokeragePerOrder, perOrderTurnover * maxPct) * orders`. Real Indian brokerage (e.g. Zerodha ₹20 or 0.03% whichever lower, PER executed order) caps each leg against that leg's OWN turnover: min(20, buyTurnover*pct) + min(20, sellTurnover*pct). When the two legs have very different turnover (a trade whose price moved a lot), the ave
  - **Fix:** Compute brokerage per leg against each leg's turnover: brokerage = min(brokeragePerOrder, buyTurnover*maxPct) + min(brokeragePerOrder, sellTurnover*maxPct) for the 2-order case (generalise if orders>2 by splitting turnover across the real orders). Add a golden row with lopsided legs to lock it.

- **F&O turnover statement silently drops commodity (COMM) and currency (CDS) derivatives — understates the 44AB audit-threshold turnover** — `src/lib/tax/turnover.ts:60,162-163` _(logic-charges-tax, medium)_
  - isFno = segment==="FUT" || segment==="OPT"; fnoTurnover filters trades by isFno, so absoluteProfitTurnover (documented as 'the figure used to test the audit / 44AD thresholds') and notional/sell turnover include only equity FUT/OPT. But commodity (COMM, MCX/NCDEX) and currency (CDS) derivatives are also NON-speculative business — classifyTaxBucket() correctly puts them in 'non-speculative-business', and the CSV labels that head 'F&O / commodity / currency'. A commodity/currency-only trader there
  - **Fix:** Either broaden the turnover statement to all non-speculative-business segments (FUT/OPT/COMM/CDS) for the audit-threshold figure, or relabel it explicitly 'equity F&O only' and add a separate commodity/currency turnover line so the audit-threshold figure isn't understated. Update fnoTurnover's filter accordingly.

- **rHistogram bucket ordering breaks for the ≤-2R / ≥3R buckets (parseFloat → NaN), so the histogram X-axis order depends on trade arrival order** — `src/lib/stats/stats.ts:210` _(logic-stats-horizon, high)_
  - rHistogram sorts buckets with `(a, b) => parseFloat(a) - parseFloat(b)`, but two of the bucket labels are `"≤ -2R"` and `"≥ 3R"`. parseFloat on a string that does not start with a numeric character returns NaN (`parseFloat("≤ -2R")` === NaN, `parseFloat("≥ 3R")` === NaN). Array.sort comparators that return NaN perform no reordering for those elements, so the two catch-all buckets are NOT guaranteed to land at the extremes — they stay wherever they were first inserted, which is driven by the orde
  - **Fix:** Give the two sentinel buckets explicit sort ranks rather than relying on parseFloat of the label. E.g. map `"≤ -2R" → -Infinity` and `"≥ 3R" → +Infinity` before comparing, or sort by a separate numeric key captured at label time (the bucket's lower edge), not by re-parsing the display string.

- **runSimulation produces all-NaN results when rSamples is empty (no guard inside the pure function / worker)** — `src/lib/montecarlo/simulate.ts:159` _(logic-stats-horizon, high)_
  - Inside the path loop, `const r = samples[(rand() * m) | 0]!` indexes the R-sample array. When `m === 0` (empty rSamples) this is `samples[0]` === undefined, so `equity += r` becomes NaN and propagates into every cone band, finalEquity percentile, both drawdown figures, and riskOfRuin/probNetPositive (NaN comparisons silently yield 0). runSimulation has no `m === 0` guard. The MonteCarlo React component gates at MIN_TRADES=30 R-bearing trades, so the UI path is currently protected, but runSimulat
  - **Fix:** Early-return a well-defined neutral result when `samples.length === 0` (e.g. a flat cone at startEquityR, riskOfRuin/probNetPositive based on the floor, zero drawdown), or throw a descriptive error so the worker reports it instead of posting NaN. Also consider validating that startEquityR is finite.

- **Engine ignores SL/target basis: "underlying"-basis triggers are silently computed in option-premium space** — `src/lib/backtest/engine/engine.ts:643-667` _(logic-options-backtest, high)_
  - StrategyDef accepts a two-axis trigger unit x basis where basis is z.enum(["premium","underlying"]) (strategy-def.ts:73-79). computeRiskLevel only reads trig.unit and trig.value and always builds the level off entryFill (the option premium); it never branches on trig.basis. So a config with stopLoss {unit:"pts", basis:"underlying", value:20} produces a stop of "entryFill +/- 20 (option points)" instead of an underlying-distance stop -- a materially different and silently wrong level (e.g. a 20-p
  - **Fix:** Either reject basis:"underlying" at config validation until spot-referenced stops are implemented, or implement underlying-basis by tracking per-bar spot and converting the underlying move into the option-price stop. At minimum, raise a typed error / RunResult flag when basis==="underlying" so results are not silently mis-stopped.

- **Multi-leg COMM/CDS recompute applies the trade-row option_type to every leg, ignoring per-leg option_type — breaks idempotency and mischarges mixed future+option strategies** — `src/features/trades/recompute.ts:82-97` _(logic-trades-segments, high)_
  - recomputeTradeCharges derives commodityOption (line 82) and isOption (line 96) ONCE from trade.option_type, then applies the same flag to every leg in the loop (lines 85-98). But trade.option_type only reflects LEG 1 (save-statements.ts:40 writes values.optionType; trade_legs rows carry their own option_type, types.ts:85). The form/save path (utils.ts:64) and CSV path correctly compute commodityOption PER LEG via leg.optionType. So for a multi-leg COMM (or CDS) trade mixing a future leg (option_
  - **Fix:** chargeLegsForTrade should carry each leg's option_type into the ChargeLeg, and recomputeTradeCharges should derive commodityOption/isOption per leg (segment==='COMM' && leg.optionType!=null), exactly as utils.ts and csv.ts do, instead of from the trade row.

- **"NCDEX:" symbol prefix survives adapter normalization → leading-space-corrupted symbol downstream** — `extension/src/brokers/groww.ts:62` _(logic-community-extension, high)_
  - The leading-exchange-prefix strip in every adapter's instrument normalizer — groww.ts:62, dhan.ts:68, upstox.ts:55, fyers.ts:71 (and kite via EXCHANGE_TOKENS) — uses ^(?:NSE|BSE|NFO|BFO|MCX|CDS|NCD|BCD)(?::\s*|\s+). For a broker header rendered as "NCDEX: GUARSEED10", the alternation tries "NCD" then requires ':' or whitespace, but the next char is 'E', so nothing matches and the "NCDEX:" prefix is NOT stripped. The symbol "NCDEX: GUARSEED10" is then handed downstream. parseContractName (instrum
  - **Fix:** Include NCDEX in the prefix-strip alternation (same fix as the P1 token addition) so "NCDEX:"/"NCDEX " is removed at the adapter; additionally make parseContractName trim the symbol after stripping the exchange prefix (instrument-parse.ts ~line 182, e.g. s = s.replace(prefixRe, '').trim()) so a space-after-colon can never leak a leading-space symbol.

- **Public /community home page has no <h1>** — `src/app/community/community-home.tsx:1` _(seo, high)_
  - The /community route renders CommunityHomePage; community-home.tsx contains no <h1> (verified: only h2s exist, at lines 278 and 287, and those are inside dialogs/cards). The page's <title> is "Community" but the document body has no top-level heading, which is both an accessibility (heading-hierarchy) and on-page-SEO gap for a primary indexable landing surface. The site header uses a Logo link, not an h1 (src/components/shared/site-header.tsx:37), so no h1 is contributed by chrome either.
  - **Fix:** Add a single visible (or visually-hidden sr-only) <h1>, e.g. "TradeMarkk Community" / "Trader community feed", at the top of the feed column in community-home.tsx so each public community surface has exactly one descriptive h1.

- **Community feed post images render as unsized <img>, causing CLS** — `src/features/community/components/post-card.tsx:394` _(web-vitals, high)_
  - User-shared chart images in the community feed are rendered as raw <img> with no width/height attributes, no aspect-ratio container, and no CSS reserving space — only `className="w-full rounded-lg border" loading="lazy"` (post-card.tsx:392-401). The stored data-URLs have arbitrary aspect ratios: compressImage (src/lib/images.ts:7-9) only caps the longest side at 1400px and preserves the source aspect ratio, and the dimensions are never persisted (types.ts:71 stores images as plain string[]). So 
  - **Fix:** Reserve space before the image loads: wrap each image in a fixed-aspect-ratio box (e.g. a container with `aspect-video` / `aspect-[4/3]` and object-cover/contain on the img), or capture and persist the natural width/height at compress time in compressImage and set them as width/height attributes so the browser reserves the box. Either eliminates the CLS.

- **Offline fallback page tells local/BYOD-local users a falsehood ("needs a connection to reach your database")** — `src/app/offline/page.tsx:11-14` _(pwa-offline, high)_
  - The offline page copy reads: "TradeMarkk needs a connection to reach your database. Your data is safe — reconnect and reload." (offline/page.tsx:11-14). For hosted/BYOD-remote modes that is true, but for LOCAL mode the database is in-browser IndexedDB and needs no connection at all — the only reason the user sees this page is the SW navigation strategy (finding above), not an actual data-access need. This both misleads privacy-first users and masks the real bug.
  - **Fix:** Once the app shell loads offline (fix finding 1), local/BYOD-local users should never hit this page. Until then, soften the copy to not assert that the DB itself is unreachable (e.g. "You're offline. Hosted journals need a connection; local journals work offline once the app is cached.").

- **Manifest ships SVG-only icons, no PNG / no apple-touch-icon / no maskable raster — degraded installability, broken icon on iOS** — `src/app/manifest.ts:15-18` _(pwa-offline, high)_
  - The manifest declares only two SVG icons with sizes:"any" (manifest.ts:15-18) and there are no PNG assets in /public (Glob for public/**/*.png and public/**/apple-* returned nothing). iOS Safari does not use the web-app-manifest SVG icon for the home-screen icon — it requires an `apple-touch-icon` PNG link, which is absent (no `appleWebApp`/apple-touch metadata in layout.tsx or anywhere). Many Android launchers and the Chrome install UI also expect at least one raster PNG (commonly 192px and 512
  - **Fix:** Add raster PNG icons (at minimum 192x192 and 512x512, plus a 512 maskable) to the manifest, add an `apple-touch-icon` (180x180 PNG) and `appleWebApp` metadata (e.g. via Next metadata `icons.apple` / `appleWebApp`), and set a stable manifest `id` (e.g. "/?source=pwa" or "/app/dashboard"). Keep the SVG as a progressive enhancement.

- **Backtesting recharts charts render bare SVG with no accessible name (no role/aria-label)** — `src/components/backtesting/results/hero-equity-chart.tsx:59` _(a11y, high)_
  - The analytics charts deliberately carry role="img" + a chart-aria summary (see chart-aria.ts and dashboard/equity-chart.tsx:43), but the backtesting charts do not. hero-equity-chart.tsx wraps the ResponsiveContainer in a CardContent (line 59) with no role/aria; walkforward-curve.tsx:28 and compare-overlay-chart.tsx:29 wrap in a plain <div>. Recharts emits unlabeled <svg>, so a screen-reader user gets nothing — no headline equity/drawdown/IS-vs-OOS numbers. This breaks the project's own establish
  - **Fix:** Wrap each chart container with role="img" and an aria-label built from an equity/walk-forward summary helper (extend chart-aria.ts, e.g. reuse equityCurveAriaSummary for the hero curve, and add a walk-forward / journal-compare summary). Same one-line treatment used in equity-chart.tsx:41-45.

- **Public Pulse + admin trend charts: no accessible name and not gated by prefers-reduced-motion** — `src/components/charts/trend-charts.tsx:44` _(a11y, high)_
  - DailyBars (line 32) and DailyViewsArea (line 69) render Recharts BarChart/AreaChart with no role/aria-label, so the public Pulse page (app/(marketing)/pulse) and admin overview expose unlabeled SVG to screen readers. They also never pass isAnimationActive, so Recharts runs its JS-driven entry animation; per the codebase's own note (hooks/use-media-query.ts:21-22) the global prefers-reduced-motion CSS rule cannot reach JS SVG animation — every other chart passes isAnimationActive={!reduced} or {f
  - **Fix:** Add role="img" + an aria summary (count/peak day) on the ResponsiveContainer wrapper, and gate animation: read useReducedMotion() and pass isAnimationActive={!reduced} (or accept an animate prop), matching group-bar.tsx / r-histogram.tsx.

- **tag-picker chips convey selection by color only and expose no selected state to screen readers** — `src/features/trades/components/tag-picker.tsx:36` _(a11y, high)_
  - Each tag is a <button> whose only signal of being selected is a background tint / border change (lines 40-44, inline style backgroundColor: `${t.color}26`). There is no aria-pressed (or role=option/aria-selected), so a screen-reader user cannot tell which tags are active, and the distinction is color-only for low-vision/colorblind users. Contrast with sentiment-toggle.tsx and day-time-heatmap.tsx which correctly set aria-pressed on their toggle buttons.
  - **Fix:** Add aria-pressed={active} to the chip <button> (line 36) and pair the color tint with a non-color affordance (e.g. a Check icon or visible 'selected' ring) so selection is perceivable without color.

- **No mobile navigation menu — 7 public nav destinations unreachable from the header on phones** — `src/components/shared/nav-links.tsx:21` _(responsive, high)_
  - NavLinks renders `<nav className="hidden gap-1 text-sm md:flex">` — the entire primary nav (Features, Community, Backtesting, Pulse, Docs, Blog, FAQ) is hidden below the md breakpoint (768px) and there is NO hamburger/sheet fallback. SiteHeader (src/components/shared/site-header.tsx:36) only shows Logo + ThemeToggle + Sign-in CTA on a 360/390px screen. SiteHeader is shared by BOTH the marketing layout and community public pages, so a mobile visitor on /community, /backtesting, /pulse, etc. has n
  - **Fix:** Add a mobile menu trigger (hamburger) in SiteHeader that opens a Sheet/Dialog listing the NavLinks items, shown only below md (e.g. `md:hidden`). Reuse the existing vaul Sheet. Ensure Backtesting is reachable.

- **No single spec proves the community post → comment → DM funnel end to end** — `scripts/e2e-community.mjs:41-89` _(e2e-coverage, medium)_
  - The post→comment social funnel is covered (e2e-community.mjs creates a user, posts, likes, comments, replies, deletes) and DM is covered separately (e2e-dm-v2.mjs spins up two fresh users A/B and exercises send/seen/typing/react/edit/delete/block). But the task's named critical flow 'community post→comment→DM' is never exercised as one connected journey: there is no spec where user A reads B's post, comments, then opens a DM to B from that context. The hand-off surfaces (e.g. 'Message' button on
  - **Fix:** Add scripts/e2e-social-funnel.mjs: two synthetic users; A posts → B comments → A opens a DM to B from the author/profile entry point → message delivered. Reuse the throwaway-user setup + DB cleanup pattern from e2e-dm-v2.mjs. Keep it in the nightly job.

- **Mode-switching coverage requires real BYOD Turso creds and is excluded from any automatable run** — `scripts/e2e-byod-switch.mjs:19-22` _(e2e-coverage, medium)_
  - The only spec exercising storage mode-switching (the dual-mode hosted/BYOD/local pivot central to the product) is e2e-byod-switch.mjs, which `process.exit(1)`s immediately unless BYOD_URL and BYOD_TOKEN env vars point at a real, writable user-owned Turso database. That means it can never run in CI or an unattended nightly without provisioning live external credentials, and in practice it only runs when a developer manually supplies a personal DB. The /api/mode/switch endpoint and the local↔hoste
  - **Fix:** Split a no-external-creds variant scripts/e2e-mode-switch.mjs that covers the local↔hosted transitions using only an ephemeral platform Turso DB (the same one e2e-auth/account-settings already use), asserting tm.mode persistence and that journal reads stay client-side after each switch. Keep the full BYOD round-trip as the manual e2e-byod-switch.mjs but document it as creds-gated and out of the au


### P3 (37)

- **Banned-user gate is inconsistent — applied to create/edit-post but not to comment edit or any reaction/bookmark/pin/like/share** — `src/app/api/community/comments/[id]/route.ts:20-34` _(sec-community-content, high)_
  - isUserBanned() gates POST create-post (posts/route.ts:59), PATCH edit-post (posts/[id]/route.ts:218), POST comment (posts/[id]/comments/route.ts:18) and reshare (posts/[id]/reshare/route.ts:24). It is NOT checked in: comment PATCH/DELETE (comments/[id]/route.ts), comment-like (comments/[id]/like/route.ts), post like (posts/[id]/like/route.ts), bookmark, pin, share, report, tag-follow. A suspended account can still edit its existing comments (changing already-published text post-suspension), like
  - **Fix:** Decide the policy explicitly and centralize it. At minimum add the isUserBanned 403 gate to comment PATCH (it is the one path that lets a banned user publish new text). A shared requireActiveUser(session) helper used by all state-changing community routes would remove the drift.

- **Feed free-text search (?q=) does not LIKE-escape user input, unlike the dedicated /search route** — `src/server/community.ts:801-803` _(sec-community-content, high)_
  - buildFeedConditions builds `const like = "%"+q.search.slice(0,60)+"%"` and pushes `posts.body LIKE ${like} OR posts.title LIKE ${like}` with no escapeLike(). The value is parameterized (no SQL injection), but LIKE metacharacters % and _ are interpreted, so `?q=%` or `?q=_` matches every post and turns the search filter into a full scan with attacker-controlled wildcards. The sibling /search route (src/app/api/community/search/route.ts:14,39-40) and /autocomplete (autocomplete/route.ts:10) and th
  - **Fix:** Escape the search term the same way as the search route: `const like = "%"+escapeLike(q.search.slice(0,60))+"%"` and add `ESCAPE '\\'` to the LIKE clauses (escapeLike is already exported from this module at line 438).

- **Origin/CSRF check is bypassed when the Origin header is absent on state-changing routes** — `src/server/origin-check.ts:11-12` _(sec-community-content, medium)_
  - isAllowedOrigin() returns true whenever the Origin header is missing (`if (!origin) return true`). Every state-changing community route relies on this single function for CSRF defense-in-depth (posts POST/PATCH/DELETE, comments, like, bookmark, pin, reshare, share, report, tag-follow). A client that omits Origin (non-browser tooling, and historically some same-origin/no-CORS edge cases) skips the check entirely. The real CSRF protection is Better Auth's SameSite=Lax session cookie (noted in the 
  - **Fix:** If treating this as a real CSRF control, require a valid Origin (or Sec-Fetch-Site) on state-changing methods rather than allow-on-absent; otherwise document explicitly that SameSite=Lax is the sole CSRF control and this is best-effort hardening only.

- **IP-keyed rate limits are bypassable via a spoofed first X-Forwarded-For hop** — `src/server/client-ip.ts:11` _(sec-community-social, medium)_
  - `clientIp()` returns `(x-forwarded-for ?? '').split(',')[0].trim()` — the FIRST hop, which is fully attacker-controlled (a client can send any `X-Forwarded-For` value; the platform appends the real edge IP at the END of the chain, not the start). The leaderboard route (src/app/api/community/leaderboard/route.ts:19) keys its anti-scrape limiter on this value, so an attacker can rotate the header to defeat the 10/min cap entirely. Same pattern is used for the search/autocomplete/unfurl IP limiters
  - **Fix:** On Vercel, derive the client IP from the trusted last hop or the platform-injected header (e.g. `x-real-ip` / `x-vercel-forwarded-for`, or take the LAST element of `x-forwarded-for` that the platform controls) rather than the first, client-supplied hop. Document the trust boundary in client-ip.ts.

- **Cold-start starter-author suggestions can recommend banned users and users who blocked the viewer** — `src/server/community.ts:1191-1198` _(sec-community-social, medium)_
  - `getStarterSuggestions` builds the 'popular authors' list from `SELECT p.username,... FROM profiles p JOIN (SELECT user_id, COUNT(*) FROM posts GROUP BY user_id) po ... ORDER BY po.posts DESC LIMIT 12` with no exclusion of banned accounts (`user.status = 'banned'`) and no block filter in either direction. By contrast `queryFollowSuggestions` (src/server/community.ts:1980-1982, 2012-2013, 2035-2036, 2050-2051) correctly excludes self, both-direction blocks, and banned users. The starter surface (
  - **Fix:** Add `AND user_id NOT IN (SELECT id FROM user WHERE status='banned')` and both-direction block exclusions (`blocker_id = viewerId` and `blocked_id = viewerId`) to the popular-authors query in getStarterSuggestions, mirroring queryFollowSuggestions.

- **Expensive personalized discovery GETs have no per-user rate limit** — `src/app/api/community/foryou/route.ts:11-20` _(sec-community-social, low)_
  - `foryou` (queryForYou: interest-profile build + candidate scan up to FORYOU_CANDIDATE_CAP + facet loads + JS ranking) and `who-to-follow` (queryFollowSuggestions: 4+ candidate-source queries + enrichment fan-out) and `suggestions` (getStarterSuggestions: trending recompute + leaderboard) are uncached, recomputed per request, and have NO rate limit. The leaderboard route deliberately rate-limits for exactly this reason (its own comment at leaderboard/route.ts:18). An authenticated user can loop t
  - **Fix:** Add a modest per-user limiter (e.g. `rateLimit('foryou:'+userId, 60, 60)` and similar for who-to-follow/suggestions) consistent with the existing leaderboard/search precedent.

- **Backtest run-result blob is stored verbatim with no size bound (storage-exhaustion vector)** — `src/features/backtest/shared/run-result.ts:144-149` _(sec-backtest-blog, high)_
  - runResultSchema contains several unbounded arrays (qualityChips, equityCurve, monthlyReturns, tradeReturns, blotter, perLeg) and unbounded strings (e.g. qualityChip.label, blotterRow.day) with no .max() limits. POST /api/backtest/runs (src/app/api/backtest/runs/route.ts:30-43) parses this body with zod and then saveRun (src/server/backtest.ts:73-82) writes serializeRunResult(result) into the platform DB VERBATIM with no content-length check and no per-blob size cap. Next.js App Router Route Hand
  - **Fix:** Add explicit bounds to runResultSchema: cap the large arrays (e.g. blotter/equityCurve/tradeReturns .max(N) consistent with the engine's max date range, qualityChips .max(~12), perLeg .max(8)) and add .max() to free-text fields (qualityChip.label, etc.). Additionally, enforce a hard request-body size limit in POST /api/backtest/runs by checking the Content-Length header (or reading the raw text an

- **Hosted DB token labelled "short-lived" is actually 7-day full-access** — `src/server/turso-platform.ts:52` _(sec-platform, medium)_
  - mintDbToken creates a token with expiration "7d" and authorization "full-access". The route doc-comment (src/app/api/db/token/route.ts:11) and turso-platform.ts:34 both call it "short-lived", and getHostedConnection caches it in sessionStorage for 24h (src/providers/db-session-provider.tsx:51-77). This is NOT a true vulnerability: the token is correctly single-database scoped (createToken(dbName,...)) and gated by an ownership check (mintDbToken verifies dbName belongs to userId at turso-platfor
  - **Fix:** Reduce the token lifetime to match the 24h client cache (or shorter, e.g. 1h with silent re-mint), and update the "short-lived" comments to state the real TTL. The client already transparently re-mints on 404/expiry, so a shorter TTL costs little. Consider scoping to read-only where the client only reads.

- **Hosted journal tokens are 7-day full-access, not truly short-lived** — `src/server/turso-platform.ts:52` _(db-tenant-isolation, high)_
  - mintDbToken issues a Turso token with expiration:"7d" and authorization:"full-access". The token is then cached client-side for 24h in sessionStorage (db-session-provider.tsx:73-76) and in chrome.storage.session for the extension (extension/src/lib/app-api.ts:91). SECURITY.md correctly and honestly documents this as "short-lived (7-day)", so there is no misrepresentation, and the token's blast radius is strictly the user's OWN isolated database (the org/platform API token never leaves the server
  - **Fix:** Consider shortening the token lifetime to ~24h (matching the existing client cache TTL, so no extra mint pressure) and/or minting read-only tokens for read-heavy surfaces. The mint route is already tightly rate-limited (10/hr, never two within 10min), so a shorter expiry would not materially increase mint volume given the 24h cache.

- **getStarterSuggestions popular-authors subquery does an unbounded full GROUP BY over all posts** — `src/server/community.ts:1191-1198` _(perf-server-queries, medium)_
  - The starter-suggestions popular-authors source runs `SELECT ... FROM profiles JOIN (SELECT user_id, COUNT(*) AS posts FROM posts GROUP BY user_id) ... ORDER BY posts DESC LIMIT 12`. The inner aggregate has NO time window and NO LIMIT, so it counts EVERY post in the table grouped by user before the outer LIMIT 12. idx_posts_user exists but a full GROUP BY still scans all rows. This runs for low-signal (new) viewers, exactly the cohort hitting onboarding. It is wrapped in a try/catch that degrades
  - **Fix:** Bound the inner aggregate to a recency window (e.g. `WHERE created_at >= since`) like the follow-suggestions popularRows query (community.ts:2044-2056), and/or wrap the popular-authors result in `cached()` for a few minutes since it is identical across all cold-start viewers. queryFollowSuggestions already windows its cold-start popular query — make this one consistent.

- **countNewerPosts ("N new posts" pill) reuses LIKE/json_each feed conditions in a COUNT over an unindexed predicate set** — `src/server/community.ts:907-925` _(perf-server-queries, medium)_
  - The live "N new posts" pill calls countNewerPosts, which reuses buildFeedConditions (including the `tags LIKE '%"tag"%'` scan, the `json_each` following-tag EXISTS, and the watchlist post_symbols EXISTS) plus `created_at > since`. While `created_at > since` is index-backed and the window is tiny in steady state, when a tag/scope filter is active the COUNT must evaluate the same unindexable LIKE/json_each predicate across the candidate window. This pill is polled periodically by the client, so th
  - **Fix:** Once a normalized post_tags table exists (see the tag-LIKE finding), this COUNT inherits the indexed lookup. Until then, consider capping the count subquery (e.g. `SELECT COUNT(*) FROM (SELECT 1 FROM posts WHERE ... LIMIT 51)`) so the predicate stops evaluating once the cap is reached, and ensure the poll interval is conservative.

- **Dashboard recomputes closedOnly/dailyPnl/horizonMix unmemoized on every render** — `src/app/app/dashboard/page.tsx:67` _(perf-client, medium)_
  - Lines 67-75 run on every render without useMemo: `closedOnly(allTrades)` (full filter pass), `dailyPnl(allClosed)` (O(n) aggregation, stats.ts:95), and `horizonMix(allClosed)` (O(n) per-trade classifyHorizon pass, horizon.ts:166). The period-filtered `trades` set IS memoized (line 41) but these full-set derivations are not, so any state change (e.g. theme/dropdown/router state) re-runs three O(n) passes over the entire journal on the main thread. The calendar page has the same pattern: `dailyPnl
  - **Fix:** Wrap allClosed/monthPnl/mix (dashboard) and pnlMap/monthTotal (calendar) in useMemo keyed on [allTrades]/[trades], matching how the analytics and insights pages already memoize their derivations.

- **Same-day CNC equity classified inconsistently: speculative (classifyTrade) vs capital-gains (classifyTaxBucket/isIntradayEquity)** — `src/lib/tax/turnover.ts:53-58,79-86` _(logic-charges-tax, medium)_
  - classifyTrade() (legacy two-way) calls a same-IST-day equity round trip 'speculative' purely on sameIstDate, regardless of product. classifyTaxBucket() via isIntradayEquity() hard-returns false for product==="CNC"/"BTST"/"STBT" (line 82) BEFORE the same-day check, so a CNC trade opened and squared off the SAME IST day is bucketed as capital-gains, not speculative. The two functions thus disagree for the identical trade. This also diverges from the repo's own horizon module (commit cfbb757 / src/
  - **Fix:** Make isIntradayEquity consistent with classifyHorizon/classifyTrade: check same-IST-day FIRST (intraday/speculative) and only fall to delivery/capital-gains for genuinely overnight CNC/BTST/STBT. Or delete the now-unused legacy classifyTrade/speculativeSplit to remove the divergent definition.

- **F&O turnover uses NET (after-cost) P&L for the ICAI absolute-profit convention instead of gross** — `src/lib/tax/turnover.ts:170-176` _(logic-charges-tax, low)_
  - fnoTurnover uses settle = t.net_pnl (net of brokerage+taxes) as the favourable/unfavourable 'settlement' summed into absoluteProfitTurnover. The ICAI Guidance Note absolute-profit turnover convention sums the GROSS favourable/unfavourable differences (before costs); netting costs in slightly understates turnover and the audit-threshold test. The code comment acknowledges this as a deliberate 'conservative after-cost basis', so it is a documented convention choice rather than an outright bug, but
  - **Fix:** Use gross_pnl for the absolute-profit turnover (matching the ICAI gross convention) and keep net only for the netRealised line, or relabel the figure to make the after-cost basis explicit so it isn't read as the standard ICAI turnover.

- **rHistogramAriaSummary appends a duplicate 'R' to a bucket label that already ends in 'R' (screen-reader announces e.g. '0.5RR')** — `src/features/analytics/chart-aria.ts:42` _(logic-stats-horizon, high)_
  - rHistogram returns buckets whose `bucket` field already includes the unit, e.g. `"0.5R"`, `"≤ -2R"`, `"≥ 3R"` (stats.ts:198-201). The aria summary builds `Most common outcome: ${peak.bucket}R ...`, yielding `"0.5RR"` / `"≤ -2RR"`. This is the accessible label a screen reader reads aloud for the R-distribution chart, so the announced value is wrong/garbled.
  - **Fix:** Drop the trailing `R` in the template (`Most common outcome: ${peak.bucket} with ...`) since peak.bucket already carries the unit.

- **Within src/lib/stats, equity-curve / daily-P&L day bucketing uses UTC date slicing while horizon classification uses IST — internal timezone inconsistency** — `src/lib/stats/stats.ts:99` _(logic-stats-horizon, medium)_
  - dailyPnl keys days by `t.closed_at.slice(0, 10)`, i.e. the raw UTC calendar date, and equityCurve (line 68) is built on top of it. In the same lib/stats directory, horizon.ts deliberately classifies and counts holding periods on the IST calendar date via istDateKey (horizon.ts:96, fy.ts:15-19), and the analytics page mixes both on the same `closed` set. For normal NSE/BSE closes (≤15:30 IST = 10:00 UTC) the UTC date equals the IST date, so it is harmless in practice; but any trade closed between
  - **Fix:** Key dailyPnl (and the equity-curve baseline) by istDateKey(t.closed_at) to match horizon.ts and the tax/FY grouping, so all of lib/stats agrees on the IST close date. Add a regression test with a post-18:30Z close.

- **barsExpected/barsPresent (filledBarFraction) accumulate for days that are filtered out or excluded** — `src/lib/backtest/engine/engine.ts:234-251` _(logic-options-backtest, medium)_
  - barsExpected += SESSION_MINUTES and barsPresent += dd.index.length run for EVERY spine day BEFORE replayDay decides the day is filtered (daysFromExpiry / no entry bar -> null, continue) or excluded (MISSING_LEG -> continue). Those non-traded days still inflate both the numerator and denominator of coverage.filledBarFraction (engine.ts:314), so the reported "filled bar fraction" mixes in days the strategy never traded. This is a coverage-honesty/reporting inaccuracy (the headline fill fraction no
  - **Fix:** Accumulate barsExpected/barsPresent only for days that actually book a row (after the null/excluded guards), or compute filledBarFraction over booked days only, so the coverage metric describes the traded sample.

- **daysFromExpiry filter uses enabledLegs[0].expiry while the day's data + spine expiry use config.legs[0].expiry** — `src/lib/backtest/engine/engine.ts:232 vs engine/engine.ts:441-444` _(logic-options-backtest, medium)_
  - The replay's data fetch and spine resolve expiry from config.legs[0].expiry (engine.ts:232), but the daysFromExpiry entry filter in replayDay resolves tradingDaysToExpiry from enabledLegs[0].expiry (engine.ts:442). If config.legs[0] is disabled (or legs carry different expiry rules), the data is loaded for one expiry while the daysFromExpiry gate is evaluated against a different one, so days can be filtered/kept against an expiry that does not match the contract actually traded. Single-expiry st
  - **Fix:** Use a single, consistent expiry source for both data fetch and the daysFromExpiry filter (prefer enabledLegs[0] for both, since disabled legs should not drive expiry), or explicitly require all enabled legs share an expiry rule and assert it.

- **Zerodha/generic CSV time parsing uses native new Date(), which misreads day-first broker timestamps** — `src/features/trades/csv.ts:91-92` _(logic-trades-segments, medium)_
  - rowsToFills (used by the Zerodha spec via csv-brokers.ts:302 and the manual-mapping fallback) parses time with new Date(rawTime).toISOString(). Verified: new Date('12-06-2026 09:21:34') yields 2026-12-06 (month/day swapped) and new Date('12/06/2026 ...') likewise. All OTHER broker specs route through the day-first-aware parseTimestamp (instrument-parse.ts). Zerodha's documented order_execution_time is ISO-like ('2026-06-12 09:21:34') and parses correctly, so this is latent rather than firing for
  - **Fix:** Route rowsToFills through parseTimestamp(dateStr, timeStr) like the other broker mappers, instead of new Date(rawTime), so day-first dates are parsed deterministically across the Zerodha and manual-mapping paths.

- **Reputation per-reactor cap is per-tally, not per-distinct-reactor — duplicate reactorId rows bypass the anti-sock-puppet guard** — `src/features/community/reputation.ts:266` _(logic-community-extension, medium)_
  - cappedReactionUnits (reputation.ts:266-277) iterates the ReactorTally[] and applies REACTIONS_PER_REACTOR_CAP (3) to each tally's count independently. If the same reactorId appears in more than one tally row, each row earns up to the cap, so a single reactor split across N rows contributes up to N×3 units — defeating the per-reactor cap the module advertises as its core anti-sock-puppet defense-in-depth (reputation.ts:19-30 "the model also defends in depth by capping per-reactor contribution"). 
  - **Fix:** In cappedReactionUnits, aggregate counts per distinct reactorId BEFORE applying the cap (e.g. build a Map<reactorId, sumCount>, skip selfId, then sum Math.min(total, perReactorCap) over the map). This makes the per-reactor cap true regardless of how the server groups rows.

- **Per-route canonical pages don't set openGraph.url, so og:url falls back to the homepage** — `src/app/(marketing)/features/page.tsx:5-10` _(seo, high)_
  - The root layout sets a static openGraph.url = siteConfig.url (the homepage) at src/app/layout.tsx:31. Routes that set `alternates.canonical` but not `openGraph.url` — /features (features/page.tsx:9), /faq (faq/page.tsx:7), /privacy (privacy/page.tsx:7), /terms (terms/page.tsx:7), /changelog (changelog/page.tsx:6), /compare/tradezella-alternative (compare/.../page.tsx:9) — inherit og:url = homepage. The /community and /blog pages already noted this and added `openGraph: { url: "/community" }` (co
  - **Fix:** Add `openGraph: { url: "<route>" }` (relative; metadataBase resolves it) to each marketing page's metadata, matching the canonical, as already done in community/page.tsx.

- **Twitter card has no site/creator handle** — `src/app/layout.tsx:45-50` _(seo, medium)_
  - The root twitter metadata sets card/title/description/images but no `site` or `creator` handle. Without an @handle, Twitter/X attribution is unset on every shared link. Low impact and optional, but a quick win if a brand handle exists.
  - **Fix:** If a brand X handle exists, add `site: "@trademarkk"` (and optionally creator) to the twitter metadata in layout.tsx. If no handle exists yet, leave as-is.

- **Personal community pages (messages, notifications) are indexable with the generic "Community" title/canonical** — `src/app/community/messages/page.tsx:1` _(seo, medium)_
  - messages/page.tsx and notifications/page.tsx are "use client" pages with no metadata export, so they inherit the layout's `title: "Community"`, `alternates.canonical: "/community"`, and crucially no `robots: noindex`. These are auth-gated personal screens that should never be indexed; nothing marks them noindex. They are not in the sitemap, and their content is behind a sign-in gate, so crawl risk is low, but the correct signal is missing (compare blog/write/page.tsx:11 which correctly sets `rob
  - **Fix:** Add `export const metadata = { robots: { index: false } }` to a small server wrapper for messages and notifications (or set it via a shared segment), so personal community surfaces are explicitly noindex rather than inheriting an indexable "Community" canonical.

- **motion (framer-motion) loaded on the landing critical path via the hero showcase** — `src/app/(marketing)/_components/hero-showcase.tsx:5` _(web-vitals, medium)_
  - The landing hero showcase is a "use client" component that imports motion/react (hero-showcase.tsx:5) and @number-flow/react (line 4) and is rendered eagerly above the fold inside the hero section (page.tsx:147). motion is a large dependency (~tens of KB gzipped) and sits in the landing page's initial client bundle, contributing to TBT/INP on the most-trafficked public page. It is not in Next 15's default experimental.optimizePackageImports list (next.config.ts has no experimental block at all).
  - **Fix:** Either (a) add experimental.optimizePackageImports: ['motion','recharts','@number-flow/react'] to next.config.ts to trim barrel imports, and/or (b) lazy-mount HeroShowcase with next/dynamic and a sized placeholder skeleton (it occupies a fixed max-w-3xl box, so a reserved-height skeleton avoids CLS) so motion/NumberFlow leave the landing's initial JS. The CSS-only rise-in animations elsewhere alre

- **No install prompt / A2HS UX — beforeinstallprompt is never captured** — `src/components/pwa-register.tsx:10-29` _(pwa-offline, high)_
  - PwaRegister only registers/unregisters the SW (pwa-register.tsx:10-27). A repo-wide grep for `beforeinstallprompt`, `appinstalled`, `getInstalledRelatedApps`, and `prompt()` found no app code handling install (only extension/build artifacts). There is no custom install button, no deferred-prompt capture, and no iOS "Add to Home Screen" hint — so installability relies entirely on the browser's default mini-infobar, which Chrome increasingly suppresses, and iOS gives no prompt at all. For a produc
  - **Fix:** Capture `beforeinstallprompt` (preventDefault + stash the event), surface a lightweight "Install app" affordance in the app shell that calls deferredPrompt.prompt(), and show an iOS-specific A2HS hint (detect standalone + iOS) since iOS lacks the event.

- **Service worker cache VERSION is a hardcoded constant — precached /offline and icons can go stale indefinitely** — `public/sw.js:2` _(pwa-offline, medium)_
  - VERSION = "tm-v1" is a single static string (sw.js:2) used for both precache and runtime cache, and the activate handler only deletes caches whose key !== VERSION (sw.js:14-21). Runtime-cached `/_next/static/*` are content-hashed so staleness there is self-correcting, but the PRECACHE entries (`/offline`, the two icons) are cache-first with no revalidation and are only refreshed when VERSION changes. Since VERSION is hand-edited and the build never bumps it, an updated offline page or icon will 
  - **Fix:** Derive VERSION from a build-time value (e.g. inject the build id / git sha at build, or template the SW) so each deploy invalidates the precache, or move `/offline` to a stale-while-revalidate strategy so it self-heals.

- **Storage/theme dropdown trigger is icon-only with no accessible name** — `src/components/layout/topbar.tsx:135` _(a11y, high)_
  - The DropdownMenuTrigger renders <Button variant="ghost" size="icon"> containing only <ModeIcon> (no aria-label, no sr-only text). Radix gives it role=button but its accessible name is empty, so a screen reader announces 'button' with no purpose. Sibling icon triggers in the app (notifications-bell.tsx:43, bottom-nav.tsx:64, theme-toggle.tsx:25) all carry aria-label.
  - **Fix:** Add aria-label (e.g. `Storage and theme settings`) to the Button at line 135.

- **Calendar month prev/next icon buttons have no accessible name** — `src/app/app/calendar/page.tsx:95` _(a11y, high)_
  - The two size="icon" buttons at lines 95 and 101 contain only <ChevronLeft/> and <ChevronRight/> with no aria-label; the visible month label between them is a separate <div> not associated with the controls. Screen readers announce two unlabeled buttons. (Note the in-house date-time-picker MonthNav at date-time-picker.tsx:96/107 does this correctly with aria-label 'Previous month'/'Next month'.)
  - **Fix:** Add aria-label="Previous month" / "Next month" to the two buttons.

- **Journal day prev/next navigation buttons have no accessible name** — `src/features/journal/components/journal-editor.tsx:109` _(a11y, high)_
  - The day-navigation buttons (line 109 Previous, lines 121-133 Next) are size="icon" links/buttons containing only ChevronLeft/ChevronRight with no aria-label; the date text between them is an unrelated <div>. Screen-reader users get two unlabeled controls and cannot tell they move between journal days.
  - **Fix:** Add aria-label="Previous day" / "Next day" (the disabled Next branch at line 132 also needs the label).

- **Trade-detail back button has no accessible name** — `src/features/trades/components/trade-detail.tsx:135` _(a11y, high)_
  - The back control is <Button variant="ghost" size="icon" asChild><Link href="/app/trades"><ArrowLeft/></Link></Button> with no aria-label and no sr-only text, so its accessible name is empty. (message-thread.tsx:143 implements the same back pattern correctly with aria-label="Back to conversations".)
  - **Fix:** Add aria-label="Back to trades" on the Link (or Button).

- **Sidebar collapse toggle has no accessible name** — `src/components/layout/sidebar.tsx:98` _(a11y, high)_
  - The collapse/expand button contains only <PanelLeftOpen/>/<PanelLeftClose/> with no aria-label. Unlike the nav links in the same file (which are wrapped in Tooltip when collapsed), this control has no tooltip and no label, so its accessible name is empty and its expanded/collapsed state is not exposed.
  - **Fix:** Add a dynamic aria-label (e.g. sidebarCollapsed ? 'Expand sidebar' : 'Collapse sidebar') and aria-expanded={!sidebarCollapsed} to the Button at line 98.

- **Sentiment-gauge window selector uses tab roles without keyboard arrow navigation or roving tabindex** — `src/features/community/components/sentiment-gauge.tsx:32` _(a11y, medium)_
  - The window switcher declares role="tablist" with role="tab" + aria-selected buttons (lines 32-48) but has no arrow-key navigation, no roving tabindex, and the tabs are not associated with a tabpanel (no aria-controls / role=tabpanel). Per ARIA tab pattern, arrow keys should move between tabs. As authored a SR user is told these are tabs but the expected keyboard behavior is absent. (The richer reaction-picker.tsx and community-search listbox implement full keyboard handling; this one does not.)
  - **Fix:** Either implement the tab keyboard pattern (ArrowLeft/Right + roving tabindex + aria-controls to a tabpanel) or, simpler, drop the tab roles and render them as a role="group" of aria-pressed toggle buttons (matching day-time-heatmap.tsx:82 metric toggles).

- **Vaul bottom Sheet sets a Title but provides no Description, unlike the Dialog primitive** — `src/components/ui/sheet.tsx:34` _(a11y, medium)_
  - SheetContent renders VaulDrawer.Title (line 34) but never a VaulDrawer.Description, and exposes no prop to supply one. The Dialog primitive pairs DialogTitle with DialogDescription and most dialogs use both (39 DialogDescription/aria-describedby usages found). Sheets that mirror the same forms on mobile therefore lose the descriptive context (aria-describedby) that the desktop Dialog provides. Focus trap/restore itself is handled by vaul, so this is a labeling gap, not a trap gap.
  - **Fix:** Add an optional description prop to SheetContent that renders a VaulDrawer.Description (sr-only acceptable) so the mobile sheet carries the same accessible description as its Dialog counterpart.

- **mobile-audit.mjs only catches page-level horizontal overflow — blind to touch targets, fixed-bar clipping, dialog vh overflow, and several routes** — `scripts/mobile-audit.mjs:69` _(responsive, high)_
  - The script's only check is `doc.scrollWidth - doc.clientWidth > 1` at two widths (360/390). That means it (a) cannot detect tap targets < 40px, content clipped under the fixed BottomNav/MobilePayoff/FABs, dialogs taller than the viewport, or tables/charts that scroll *inside* an overflow-x-auto wrapper (those report 'ok' even when unusable on a phone); (b) omits authenticated-only-shaped and dynamic routes from its lists: /admin (admin-shell.tsx), profile pages /community/u/[username], trade det
  - **Fix:** Augment the audit: after the overflow check, also assert all elements matching button/a/[role=button]/input have getBoundingClientRect height>=40 (warn list), assert no DialogContent exceeds innerHeight, and add /admin, /community/u/<seed-user>, /app/trades/<seed-id> to the route lists. Keep the existing overflow check as-is.

- **Primary post action buttons are ~28px tall — below the 40px mobile touch-target guideline** — `src/features/community/components/post-card.tsx:444` _(responsive, medium)_
  - The PostCard footer actions (comments link line 442-451, bookmark line 452-464, reshare trigger line 466-474, share line 484-493) all use `px-2.5 py-1.5 text-xs`, i.e. ~6px vertical padding + ~16px text line = ~28px tap height. The same sub-40px sizing is used for the mobile feed tab pills in community-home.tsx:221-232 (`px-3 py-1.5 text-sm`). On a 360px feed these are the most-tapped controls and sit close together, raising mis-tap risk. This is an ergonomics issue, not an overflow one, so the 
  - **Fix:** Bump interactive footer/tab controls to a min 40x40 hit area on touch (e.g. add `min-h-10` or `py-2.5` on coarse pointers, or wrap with a larger padded tap zone) without changing the visual icon size.

- **Own-message DM actions (react/edit/delete) are hidden behind hover on touch devices** — `src/features/community/components/message-thread.tsx:327` _(responsive, medium)_
  - The MessageBubble action row is `opacity-0 transition-opacity focus-within:opacity-100 group-hover:opacity-100`. Touch devices have no hover, and the row contains the only entry points to React / Edit / Delete a message. focus-within only helps after something inside is focused, but the controls are visually invisible (opacity-0) until then, so a touch user has no discoverable affordance to reveal them — they must blindly tap an invisible region. This makes editing/deleting your own DMs effectiv
  - **Fix:** On coarse pointers, keep the action row visible (e.g. `opacity-100` under a `@media (pointer: coarse)` utility or `[@media(hover:none)]:opacity-100`), or surface a persistent always-visible affordance (a small kebab on the bubble) on mobile.

- **Static legal/offline routes have no e2e coverage (terms, offline)** — `src/app/(marketing)/terms/page.tsx:1` _(e2e-coverage, high)_
  - Of the 43 page routes, /terms and /offline are referenced in zero e2e specs (grep across scripts/ for /terms, /offline returns nothing; /privacy is touched only by e2e-landing.mjs:366 in an a11y loop). These are static pages so the risk is low, but a build/route regression (e.g. broken layout import, MDX error) on a legally required page would ship silently. /backtesting/code is intentionally a placeholder ('on the way', page.tsx:23) so it correctly needs no spec.
  - **Fix:** Add /terms and /offline to the marketing path list already iterated in e2e-smoke.mjs:44-53 (h1 + 200 assertion) once the smoke spec is un-broken. Cheap, no new spec file needed.

